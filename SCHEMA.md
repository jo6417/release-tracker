# 출시 트래커 — Notion 스키마 최종안

확정일: 2026-08-05 (2026-08-17 개정)

DB 2개로 구성.

**메인 날짜는 작품 행이 직접 갖고, 부속 사건만 일정 행으로 만든다.**
정식출시·극장개봉·OTT공개·시즌시작은 작품의 `출시·개봉일`에 들어가고,
베타·데모·DLC·예약구매처럼 곁다리로 붙는 날짜만 일정 DB에 행을 만든다.
(전체 585작품 중 부속 일정이 있는 것은 20여 개뿐이다)

순차 공개 드라마는 `출시·개봉일`을 **기간**으로 적는다.
예: 쉬헐크 2022-08-17 ~ 2022-10-13

```
🎯 작품 DB
   └ 📅 일정 DB   (관계로 연결)
```

---

## 🎯 작품 DB

속성 31개. 한 번도 안 쓰인 자리는 2026-08-18에 정리했다
(한국어지원·소유상세·중복소유·후속작대기·추정시기·목표할인율·국내 출시·개봉일).

### 식별

| 속성 | 타입 | 설명 | 채우는 주체 |
|---|---|---|---|
| 제목 | 제목 | 한글 우선 (캘린더 기록 습관 유지) | 이관/수동 |
| 원제 | 텍스트 | 영문/원어 제목, API 조회용 | 스크립트 |
| 종류 | 선택 | 게임 / 영화 / 시리즈 / 만화 | 이관(스티커) + TMDB |
| 애니메이션 | 체크박스 | 매체와 직교하는 축. 애니 극장판=영화+체크, 애니 시리즈=시리즈+체크, 실사화=체크 해제 | TMDB |
| 외부ID | 텍스트 | `tmdb:movie:1234`, `tmdb:tv:5678`, `igdb:9012` | 스크립트 |
| 소개 | 텍스트 | 한 줄 줄거리. 알림에서 "이게 뭐였지"를 없애는 자리 | 스크립트 (`sync_intro.py`) |

### 진행도 (게임 / 영상)

**작품이 아니라 `나`의 상태를 적는 자리다.** 작품 쪽 상태(나왔나·완결됐나)는
`이용 가능일`·`날짜정밀도`·`방영상태`가 이미 갖고 있으므로 여기 섞지 않는다.

**매체별로 속성이 갈려 있다.** 같은 뼈대에 어휘만 다른 값을 한 속성에 넣으니
영상 쪽이 계속 어색했고(`보유함`인 드라마?), 괄호 병기는 지저분했다.
뷰가 매체별로 갈려 있어 한 화면에 둘이 같이 보이지 않는다.
중립인 셋(`미확인`·`일시 중단`·`폐기됨`)만 양쪽 같은 이름을 쓴다.

| 진행도(게임) | 진행도(영상) | 뜻 |
|---|---|---|
| **미확인** | **미확인** | 했는지 확인 안 됨 (이관분 잔여) |
| 출시 대기 | 공개 대기 | 아직 안 나옴. **출시일이 지나면 아래로 자동 전환된다** |
| 구매 대기 | — | 살 예정 |
| 보유함 | 시청 안함 | 손에 있는데(볼 수 있는데) 아직 안 함 |
| 엔딩 없음 | — | 끝이 없는 게임 (온라인·캐주얼·샌드박스). 안 한 게 아니라 끝낼 수가 없는 것 |
| 진행 중 | 시청 중 | 하는 중 / 보는 중 |
| 일시 중단 | 시청 일시 중단 | 멈췄지만 다시 할 수도 |
| 구매 보류 | 시청 보류 | 안 사기로 / 안 보기로 |
| 폐기됨(노잼) | 폐기됨(노잼) | 해보고(보고) 접음 |
| 졸업 | 시청 완료 | 끝냄. 엔딩을 봤거나, 더 파먹을 게 없어 그만뒀거나 |

`track.py`가 "이제 볼 차례" 알림을 보내는 대상은 **출시 대기 / 구매 대기 / 보유함 /
공개 대기 / 시청 안함**이다. 보류 두 개는 스스로 뺀 것이라 알리지 않는다.

`엔딩 없음`도 알림에서 통째로 빠진다 (`UNSEEN`·`PLAYING` 어느 쪽도 아니다).
원신·에이펙스 같은 걸 `보유함`에 두면 저녁 브리핑의 "오늘의 추천"에 영원히
올라온다 — 사실 안 한 게 아니라 끝이 없는 것이라 알릴 값어치가 없다.

### 대기는 자동으로 풀린다 (2026-08-20)

```
출시 대기 → 구매 대기        공개 대기 → 시청 안함
```

`track.promote_stage()`가 매일 09:00에 옮긴다. `출시 대기`는 "아직 안 나왔다"는
뜻인데 나온 뒤에도 그대로면 기대작 뷰에 눌러앉고 백로그로는 안 넘어간다.

**근거로 쓰는 날짜는 `확정`뿐이다.** `월`·`분기`면 출시·개봉일은 그 구간의 첫날이
든 자리표시자라, 지났다고 대기를 풀면 아직 안 나온 게임이 구매 대기로 올라간다.
`완결대기`는 완결일이 추정치일 수 있어 날짜 대신 **방영상태가 `완결`/`시즌완결`
인지**를 본다.
같은 문제를 Backloggery는 `Endless`, Backloggd는 `Retired`로 푼다.

만화는 아직 0건이라 속성을 만들지 않았다. 필요해지면 `진행도(만화)`를 같은
꼴로 붙이면 된다 (`읽는 중` / `완독`).

### 날짜

| 속성 | 타입 | 설명 |
|---|---|---|
| 날짜정밀도 | 선택 | 미정 / 연도 / 분기 / 월 / 확정 / **완결대기** |
| 출시·개봉일 | 날짜 | 메인 사건의 날짜. 순차 공개물은 시작~종료 기간으로 |
| | | **국내/해외를 나누지 않는다.** 아는 날짜를 넣어두고, 국내 날짜가 나오면 덮어쓴다. 덮어쓰면 `변경이력`에 남고 `[날짜변경]` 알림이 간다 |
| | | `완결대기` = 방영은 시작했지만 완결되면 몰아보려고 기다리는 중. 진행도는 `출시 대기`(`공개 대기`) 그대로 두고 여기에만 표시한다 |
| **이용 가능일** | 수식 | 정렬용 단일 기준 — 완결대기면 `완결일`, 아니면 `출시·개봉일`. "내가 볼/할 수 있게 되는 날" |

### 시리즈 완결 추적

완결되면 몰아보려고 대기 중인 작품을 매주 손으로 확인하지 않기 위한 축.
사람이 하는 건 **날짜정밀도를 `완결대기`로 두는 것** 하나뿐이고,
아래 셋은 `sync_series.py`가 TMDB로 채운다.
(진행도는 `공개 대기` 그대로다 — 추천 뷰에서 같이 본다)

`확정`이 아니므로 작품 자체는 캘린더에 안 뜨고, 완결일만 `최종화` 일정으로 뜬다.
방영 시작일이 아니라 **볼 수 있게 되는 날**이 캘린더에 있어야 맞기 때문이다.

| 속성 | 타입 | 설명 |
|---|---|---|
| 방영상태 | 선택 | 방영예정 / 방영중 / 시즌완결 / 완결 / 취소 / 휴방 |
| 방영진행 | 텍스트 | `19/24화 · 다음 2026-08-22 · 완결예정 2026-09-19` |
| 완결일 | 날짜 | 끝났으면 실제 마지막 화 날짜, 방영 중이면 예정일(또는 추정치) |

판정은 **그 행이 가리키는 시즌** 기준이다. 노션 행은 시즌 단위인데 외부ID는
시리즈 단위라, 최신 시즌만 보면 시즌1 행이 시즌3 방영 때문에 "안 끝남"이 된다.
제목의 시즌 표기 → 출시일과 맞는 시즌 → 최신 시즌 순으로 대상을 정한다.

| 상태 | 조건 |
|---|---|
| 방영중 | 남은 방영 예정 화가 있음 |
| 완결 | 전 화 방영 끝 + 마지막 시즌 + TMDB `Ended` |
| 시즌완결 | 전 화 방영 끝, 다음 시즌이 있음 |
| 취소 | TMDB `Canceled` |
| 휴방 | 화가 남았는데 다음 날짜가 안 잡힘 |
| 방영예정 | 방영일이 하나도 없음 |

### 게임 전용

| 속성 | 타입 | 설명 |
|---|---|---|
| 플랫폼 | 다중선택 | PC / PS5 / PS4 / PS3 / Switch / Switch2 / Xbox |
| 공개처 | 다중선택 | 극장 / 넷플릭스 / 디즈니+ / 왓챠 / 티빙 / 웨이브 / 쿠팡플레이 / 애플TV+ / 프라임비디오 (영상용) |
| 소유처 | 다중선택 | 스팀 / 에픽 / GOG / PS스토어 / 닌텐도 / 게임패스 / PS플러스 / 실물 |
| 획득경로 | 다중선택 | 구매 / 무료배포 / 구독포함 / 번들 |
| 게임패스 | 체크박스 | 현재 입점 여부 (스크립트 갱신) |
| 정가 | 숫자 | 할인 알림 기준은 일괄 30% (`sync_prices.py`의 상수) |
| 현재최저가 | 숫자 | 스크립트 갱신 |
| 역대최저가 | 숫자 | 스크립트 갱신 |
| 평점 | 숫자 | 메타/오픈크리틱/스팀 중 대표값 |

### 플레이 기록

| 속성 | 타입 | 설명 |
|---|---|---|
| 졸업일 | 날짜 | |
| 마지막플레이일 | 날짜 | 스팀은 자동, 나머지 수동 |
| 플레이시간 | 숫자 | 스팀 자동 |
| 개인평점 | 선택 | ★1~5 |
| 한줄평 | 텍스트 | |

### 시스템

| 속성 | 타입 | 설명 |
|---|---|---|
| 일정 | 관계 | → 일정 DB |
| 마지막확인 | 날짜 | 스크립트 |
| 변경이력 | 텍스트 | `2026-08-05: 미정 → 2026-11-19` 누적 |
| 레퍼런스 | URL | 트레일러/스토어. `sync_media.py`가 최신 영상 주소로 덮어쓴다 |
| 스티커원본 | 텍스트 | 이관 추적용 (565 등) |

---

## 📅 일정 DB

작품 하나에 여러 날짜가 붙는 경우를 전부 담는다.

| 속성 | 타입 | 설명 |
|---|---|---|
| 이름 | 제목 | `Anthem — 얼리액세스`, `프리징 105화` |
| 작품 | 관계 | → 작품 DB |
| 종류 | 선택 | 아래 참조 |
| 날짜 | 날짜 | |
| 날짜정밀도 | 선택 | 미정 / 연도 / 분기 / 월 / 확정 / **완결대기** |
| 시즌번호 | 숫자 | 드라마/애니 |
| 회차 | 숫자 | 만화/에피소드 |
| 공개방식 | 선택 | 일괄공개 / 주간공개 / 극장 / 해당없음 |
| 진행도 | 텍스트 | `5/8화` |
| 캘린더노출 | 체크박스 | .ics 내보내기 대상 여부 |

### 종류 값

**게임 계열**
- 발표 / 예약구매 / 알파테스트 / 베타테스트 / 데모 / 얼리액세스 / **정식출시** / DLC / 무료배포

**영상 계열**
- 극장개봉 / 국내개봉 / OTT공개 / 시즌시작 / 파트공개 / 최종화

**만화 계열**
- 회차

`최종화`는 날짜정밀도가 `완결대기`인 작품에 한해 `sync_series.py`가 만들고 갱신한다.
표시를 지우면(다 봤거나 관심이 꺼지면) 그 일정은 다음 실행에서 자동으로 정리된다.
(예전에 캘린더에 손으로 적던 "○○ 마지막화"가 이 자리다)
방영 중이라 날짜가 추정이면 이름 뒤에 `(추정)`, 날짜정밀도는 `월`로 둔다.

### 캘린더 노출 규칙

`정식출시`, `극장개봉`, `국내개봉`, `OTT공개`, `시즌시작` → 기본 노출
`베타`, `데모`, `예약구매`, `회차` → 기본 비노출 (필요 시 뷰에서 켜기)
`최종화` → 시리즈의 유일한 캘린더 항목. 완결일이 아직 안 지났거나 최근 6개월
안이면 `sync_series.py`가 자동으로 만든다
**시리즈는 시작일을 캘린더에 넣지 않는다** — 작품 행(종류=시리즈)과 `시즌시작`
일정은 `make_ics.py`가 내보내기에서 제외한다. 몰아보는 사람에게 의미 있는
날짜는 "언제 다 나오나"뿐이라서다 (데이터는 남고 캘린더에서만 빠진다)

---

## Notion 뷰 구성

### 작품 DB

| 뷰 | 형태 | 필터 |
|---|---|---|
| 기대작 보드 | 보드(진행도별) | 진행도(게임) = 출시 대기/구매 대기. 완결대기 작품도 여기 있다 (방영진행·완결일을 카드에 올리면 진행 상황이 보인다) |
| 대기 목록 | 표 | 날짜정밀도 ≠ 확정 (완결대기도 여기 들어온다) |
| 구매 판단 | 보드(소유상태별) | 진행도(게임) = 구매 대기 |
| 백로그 | 표 | 진행도(게임) = 보유함 AND 플레이시간 = 0 |
| 방치 | 표 | 진행도(게임) = 진행 중 AND 마지막플레이일 > 60일 전. **노션 뷰로만 남았다** — 같은 조건의 알림은 2026-08-21에 뺐다 |
| 아카이브 | 표 | 진행도(게임) = 완료됨/폐기됨 |
| **남은 게임** | 표 | 종류 = 게임 AND 진행도(게임) = 출시 대기/구매 대기/보유함. **이용 가능일 내림차순** |
| **남은 영상** | 표 | 종류 = 영화,시리즈 AND 진행도(영상) = 공개 대기/시청 가능. **이용 가능일 내림차순** |

### 일정 DB

| 뷰 | 형태 | 필터 |
|---|---|---|
| 출시 캘린더 | 캘린더 | 캘린더노출 = 예 |
| 전체 캘린더 | 캘린더 | 없음 |
| 이번 달 | 표 | 날짜 = 이번 달 |

---

## 알림 발동 규칙

### 즉시 알림 (별도 푸시)

1. 일정 DB의 `정식출시`/`극장개봉`/`국내개봉` 항목의 날짜정밀도가 확정으로 변경
2. 작품의 게임패스 체크박스가 false → true
3. 현재최저가가 `정가 × (1 - 목표할인율)` 이하 도달
   - **이때 소유처를 함께 표시** (중복 구매 방지)

### 일일 요약 (하루 1건, 변경 있을 때만)

- 날짜 변경/연기, 가격 변동, 평점 갱신, 신규 추천, 베타/데모 일정 추가

---

## 이관 매핑

| 캘린더 스티커 | → 종류 |
|---|---|
| 565, 574, 589 | 게임 |
| 504 | 영화 (일부 드라마 — 제목으로 재판정) |
| 564 | 드라마 |
| 570 중 '프리징' | 만화 |
| 그 외 | 이관 제외 |

**제목 패턴 → 일정 종류**

| 패턴 | 종류 |
|---|---|
| OBT, CBT, 오픈베타, 알파테스트 | 베타테스트 |
| 데모, DEMO | 데모 |
| 얼리액세스, 얼리엑세스 | 얼리액세스 |
| 예판, 예약구매 | 예약구매 |
| DLC | DLC |
| 시즌N, N부 | 시즌시작 / 파트공개 |
| NN 프리징 | 회차 |
| 그 외 | 정식출시 |

같은 작품의 여러 행은 제목 유사도로 묶어 작품 DB 1행 + 일정 DB N행으로 변환.
(예: Anthem VIP DEMO / OPEN DEMO / 얼리엑세스 / Anthem → 작품 1 + 일정 4)

---

## 이관 대상 최종 집계

| 분류 | 건수 |
|---|---:|
| 게임 | 350 (+판정분 3) |
| 영화·영상 | 277 |
| 만화 (프리징) | 39 |
| 드라마 | 2 |
| **합계** | **671** |

제외: 개인 일정 356, 게임 내 이벤트 23, TV 편성 5, 하드웨어 2, 도서 1
