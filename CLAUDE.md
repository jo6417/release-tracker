# release-tracker — 에이전트용 지침

전체 맥락은 `HANDOFF.md`에 있다. **새 세션은 그것부터 읽는다.**
이 파일에는 매번 지켜야 하는 규칙만 짧게 적는다.

## 노션을 고쳤으면 캘린더를 밀어라

작품 DB나 일정 DB를 **쓰기(추가·수정·삭제)** 했다면, 그 세션이 끝나기 전에
반드시 캘린더 갱신을 직접 걸어라. 사용자에게 "몇 분 뒤에 뜹니다"라고
말하고 넘어가지 말 것.

```bash
export PATH="/c/Program Files/GitHub CLI:$PATH"
GIT_TERMINAL_PROMPT=0 gh workflow run calendar.yml --repo jo6417/release-tracker
```

**왜.** 예약은 `* * * * *`로 적혀 있지만 GitHub이 지켜주지 않는다.
2026-08-22 실측으로 평균 33분, 최대 103분 간격이었다. 즉 그냥 두면
사용자 폰에 최대 한 시간 반 뒤에 뜬다. 수동으로 걸면 배포까지 1분 20초,
폰에 뜨기까지 4~5분이다. 근거는 `HANDOFF.md` 3절.

**언제 거는가.** 노션 쓰기가 다 끝난 뒤 한 번만. 여러 건을 고칠 거면
중간중간 부르지 말고 마지막에 한 번 부른다.

**안 걸어도 되는 경우.**
- 읽기만 했을 때
- 캘린더에 안 뜨는 값만 건드렸을 때 — `날짜정밀도`가 `확정`이 아니거나,
  `종류 = 시리즈`거나, 출시일이 6개월 이전 과거인 행. 판단이 서지 않으면
  그냥 걸어라. 데이터가 그대로면 커밋도 배포도 생기지 않아 손해가 없다.
- 스크립트(`sync_*.py`, `track.py`)를 러너가 아니라 로컬에서 돌려본 것뿐일 때

**확인.** 걸고 나서 결과까지 보고 싶으면:

```bash
gh run list --workflow=calendar.yml --limit 1 --repo jo6417/release-tracker
```

## 후보 번호로 답하면 그 자리에서 처리하라

저녁 브리핑 카드에는 `등록 후보`가 최대 두 건 올라간다. 각 줄 앞에 `c17`
같은 고정 번호가 붙어 있고, 사용자는 **채팅으로** 답한다. 이런 말이 나오면
곧바로 처리할 것:

- "c17 등록해줘" / "c17, c18 등록" -> `python apply_candidates.py 등록 c17 c18`
- "c19 버려" / "c19 필요없어"       -> `python apply_candidates.py 버림 c19`
- 진행도를 따로 말하면 `--진행도 "출시 대기"`를 붙인다
- 지금 뭐가 걸려 있는지 물으면 `python apply_candidates.py --목록`

노션에 행이 생기므로 **끝나고 캘린더를 밀어야 한다**(위 절 참고).

무응답은 보류다. 지우거나 대신 판단하지 말 것 — 다음 저녁에 다시 올라간다.
번호는 한 번 붙으면 바뀌지 않으니 며칠 전 카드를 보고 답해도 같은 것을
가리킨다. 이미 처리한 번호를 다시 부르면 "큐에 없다"가 나오는데, 그게 정상이다.

## 그 밖에

- `gh`는 설치·인증돼 있으나 **PATH에 없다.** 위처럼 붙여 쓴다.
  `GIT_TERMINAL_PROMPT=0`을 함께 줘야 로그인 GUI 창이 뜨지 않는다.
- 토큰은 `.env`(gitignore됨)에만 둔다. **채팅·로그·커밋에 값을 출력하지 말 것.**
  과거 노출·폐기 이력이 있다.
- 알림 문구는 구어체를 쓰지 않는다. 공식 어플 같은 톤으로 쓰고,
  추천에는 한 줄 설명을 반드시 붙인다.
- 셸 heredoc에 백슬래시가 든 내용을 넣으면 뭉개진다. `chr(92)`로 조립하거나
  줄 단위로 패치할 것. 이 프로젝트에서 여러 번 걸렸다.
