# 출시 트래커 — 인계 문서

> 이 파일 하나로 맥락 파악이 되도록 작성됨. 마지막 갱신: 2026-08-18
> 상세 정의는 `PROJECT.md`, 스키마는 `SCHEMA.md`, 사용법은 `README.md`

---

## 0. 한 줄 요약

게임·영화·시리즈의 **출시일을 자동 추적**하는 개인용 시스템.
GitHub Actions가 매일 09:00(KST) API를 조회해 변경분만 Notion에 기록하고,
Notion이 UI·캘린더를, 노션 멘션이 알림을 담당한다.

**서버 없음. 프론트엔드 없음. 앱 없음. 실행 시점에 Claude 없음.**

---

## 1. 사용자

- 조덕근 (부산, 게임 개발자, Unity)
- 감독 역할만 수행. **코딩은 전부 Claude 담당**이 전제
- 한글 UI 선호. 작품명도 한글 우선 (13년간 캘린더에 한글로 기록)
- Notion 상시 사용 중 (식단·운동·체중·구매목록 DB 보유)

---

## 2. 지금 돌아가는 상태 (완료)

### 데이터

| | |
|---|---|
| 작품 DB | **675행** — 게임 401 / 영화 241 / 시리즈 33 |
| 일정 DB | 28행 (베타·데모·DLC 등 부속 사건만) |
| 외부ID | 646건 (igdb 377 + tmdb 269) |
| 소유·플레이 | 소유처 145건, 플레이시간 139건 (총 4,765시간) |
| 가격 | 194건 (정가·현재최저가·역대최저가) |
| 단계 | 미확인 647 / 플레이중 8 / 보유 5 / 기대작 15 |

### 매일 09:00 자동 실행 (`.github/workflows/daily.yml`)

```
스팀 동기화 → 가격 추적(ITAD) → 변화 감지 → 알림 → .ics 생성 → 스냅샷 커밋
```

각 단계는 `continue-on-error`로 격리. 한 소스가 죽어도 추적은 계속된다.
2026-08-17에 수동 실행으로 전 과정 검증 완료 (초록불).

### 알림

노션 페이지에 사용자를 @멘션 → 폰 푸시. **검증 완료 (즉시 도착 확인).**
텔레그램은 쓰지 않는다.

- 즉시: 날짜정밀도 확정, 게임패스 입점, 목표가 도달(미보유 게임만)
- 일일 요약: 날짜 변경, 단계 변경, 신규, 방치(플레이중인데 60일 초과)
- 변경 없는 날은 아무 알림도 보내지 않는다

### 노션 뷰 6개 (작품 DB)

출시 캘린더 / 기대작 / 미확인 정리 / 내 라이브러리 / 백로그 / 플레이 중
열 순서: 제목 · 출시·개봉일 · 날짜정밀도 · 종류 · 플랫폼 · 공개처 · 소유처 · 플레이시간 · 마지막플레이일

주의: 노션 API는 뷰를 만들 수 없다. 뷰 작업은 브라우저 자동화로 했고,
툴바 팝업이 자동 클릭에 닫히는 문제가 있어 **열 머리글 메뉴**와
**뷰 탭 메뉴**, **표 우측 `+` 옆 `⋯`** 를 우회로로 써야 한다.

---

## 3. .ics 캘린더 구독 — 해결됨 (2026-08-18)

**네이버 캘린더에 정상 표시 확인.**

### 원인은 하나였다

`CR CR LF` 이중 변환. 파일을 줄바꿈 자동변환 모드로 열고 내용에도 CRLF를
붙여서 388줄 전부 줄 끝에 여분의 CR이 달렸다. iCalendar 파서가 파일 전체를
거부해서 구독은 등록되는데 일정이 0건이었다. 커밋 `2d65f21`에서 해결.

### 틀렸던 두 번째 가설

"git이 CRLF를 LF로 바꿔서 표준 위반이 된다"고 보고 `.gitattributes`에
`*.ics -text`를 넣었는데, **그 커밋은 배포되지도 않았다.** 지금 네이버가
정상적으로 읽고 있는 배포본은 LF다. 즉 네이버는 LF를 문제없이 받아들인다.
그래서 `*.ics text eol=lf`로 뒤집어, 실제 동작이 확인된 형식을 고정했다
(커밋 `1189a28`). 줄바꿈 종류는 원인이 아니었다.

### 반영 지연 — 측정 완료 (2026-08-18)

노션에 넣은 작품이 폰 캘린더에 뜨기까지의 각 구간을 실측했다.

| 구간 | 실측값 |
|---|---|
| push → GitHub Pages 배포 완료 | **67초** |
| 배포 → 네이버 캘린더 표시 | **3분 18초 이내** (00:58:42 배포 → 01:02 확인) |
| 참고: 1차 측정 | 13분 38초 이내 (00:38:22 → 01:52 사이 확인) |

**결론: 네이버는 병목이 아니다.** 배포만 끝나면 몇 분 안에 반영된다.
병목은 우리 쪽 배치가 하루 한 번(09:00)이라는 점 하나뿐이고,
여기서 최대 24시간이 발생한다. 나머지 구간을 다 합쳐도 5분 미만이다.

### 주기를 올리기 전에 반드시 고칠 것

`cron`을 매시로 바꾸면 최대 지연이 24시간에서 1시간대로 줄고,
리포가 public이라 Actions 비용은 0이다. 다만 **먼저 고쳐야 할 것이 있다.**

`track.py`의 `idle_check()`는 이전 스냅샷과 대조하지 않고 현재 상태만 보고
방치 목록을 만든다. 그리고 그 결과를 무조건 `daily`에 넣어 알림을 보낸다.
지금은 하루 한 번이라 티가 덜 나지만(그래도 같은 방치 알림이 매일 반복된다),
매시 실행으로 바꾸면 **같은 알림이 하루 24번 울린다.**
알림이 107건 울렸던 그 실수와 같은 종류다. 주기 변경 전에 반드시 상태 비교를 넣을 것.

한편 `.ics` 배포 경로를 raw.githubusercontent나 서버리스로 바꾸는 안은
**실익이 없다.** 배포 구간은 67초라 병목이 아니다. 2026-08-17의 5연속 실패는
GitHub 전면 장애(critical)라는 예외 상황이었다.

### 막고 있는 것 — GitHub 장애 (우리 코드 문제 아님)

2026-08-17 13:40Z 시작된 GitHub 전면 장애로 **Pages 배포가 3연속 실패**했다.

```
Failed to create deployment (status: 503) ... is githubstatus.com reporting a Pages outage?
```

장애 복구 후 아무 커밋이나 하나 push하면 배포가 재시도된다.
그때까지 배포본은 `2d65f21` 시점(43건)에 멈춰 있다 — 내용상 최신과
줄바꿈 빼고 동일해서 지금 당장의 피해는 없다.

**주의: 이 장애는 매일 09:00 자동 실행(Actions)도 막는다.**
복구 후 워크플로가 정상 도는지 한 번 확인할 것.

## 4. 자격 증명

`.env`(gitignore됨)에 6종. GitHub 시크릿에도 동일하게 필요.

| 키 | 로컬 .env | GitHub 시크릿 |
|---|---|---|
| `NOTION_TOKEN` | O | O |
| `TMDB_API_KEY` | O | O |
| `TWITCH_CLIENT_ID` / `_SECRET` | O | O |
| `STEAM_API_KEY` | O | O |
| `ITAD_API_KEY` | O | **X — 등록 필요** |

**`ITAD_API_KEY`를 GitHub 시크릿에 넣어야 매일 가격 추적이 돈다.**
https://github.com/jo6417/release-tracker/settings/secrets/actions

GitHub 시크릿은 쓰기 전용이라 값을 다시 볼 수 없다. 로컬 `.env`가 사본이다.
토큰은 채팅·로그·커밋에 절대 출력하지 말 것.

### 상수

```
Notion 부모 페이지: 3be857fe-7a80-808e-b6ad-c595ff14a3e8   (출시 트래커)
작품 DB:   3be857fe-7a80-81cb-8c69-fc96da6418f1
일정 DB:   3be857fe-7a80-8177-a7b5-ee73eb4de331
알림 페이지: 3bf857fe-7a80-810d-bc54-dfc8f08aac26
GitHub:   jo6417/release-tracker  (public — Pages 때문에 전환)
작업 폴더:  C:\Users\Javis.JAVIS\Documents\GitHub\release-tracker
캘린더 원본: C:\Users\Javis.JAVIS\Downloads\Calendar_ejrrms22_2026-08-04-232847.ics
SteamID:  76561197996869791 (vanity: jo6417)
```

`gh` CLI 인증됨 → Claude가 커밋·push 가능.
단 시스템 credential.helper가 간섭해 GitHub 로그인 GUI 창이 뜨는 일이 있다.
**`GIT_TERMINAL_PROMPT=0`을 붙여 실행**하고, 창이 떴다면 사용자에게 취소를
안내한 뒤 `gh auth setup-git`을 재실행하면 된다. (비밀번호 인증은 폐지됐으니
그 창에 입력해도 실패한다.)

---

## 5. 구조 (원안에서 바뀐 것)

원안은 모든 날짜를 일정 DB 행으로 만들었으나, 97%가 일정 1개짜리라
같은 날짜를 작품과 일정 두 곳에 쓰는 중복이 생겼다. 그래서:

- **메인 날짜(정식출시·극장개봉·OTT공개·시즌시작)는 작품 행이 직접 보유**
- 부속 사건(베타·데모·DLC·예약구매)만 일정 행 → 669행에서 28행으로
- 순차 공개물은 `출시·개봉일`을 기간으로 (쉬헐크 2022-08-17 ~ 10-13)
- 종류를 `게임/영화/시리즈/만화` 4종 + `애니메이션` 체크박스로 (조합 폭발 방지)
- 과거 기록은 `완료`가 아니라 **`미확인`** (실제로 했는지 모르므로)
- 속성명: `대표출시일` → `출시·개봉일`, `국내출시일` → `국내 출시·개봉일`

---

## 6. 자동 매칭이 안 되는 부분 — 대조표로 고정

한글 제목이 이 프로젝트의 최대 난관이었다. 표로 만들어 커밋해뒀다.

| 파일 | 내용 | 왜 |
|---|---|---|
| `steam_map.json` | 스팀 appid ↔ 작품 제목 | 스팀은 영문, 노션은 한글. API로 이을 방법 없음 |
| `game_title_en.json` | 한글 게임 제목 → 영문 218건 | IGDB 한글 검색 히트율 10% |

**새 게임을 사거나 추가하면 이 표에 한 줄 넣으면 된다.**

TMDB는 한글 검색이 되지만 띄어쓰기를 요구한다 (`맨오브스틸` 0건, `맨 오브 스틸` 1건).
붙여쓴 제목을 2~3글자로 끊어 재조회하는 방식으로 해결했다
(`adapters/tmdb.py`의 `search_smart`).

스팀 한글 검색은 아예 응답이 없고, IGDB의 한글 별칭은 '사펑', '젤다 야숨' 같은
은어뿐이어서 못 쓴다. 이미 시도해봤으니 다시 파지 말 것.

---

## 7. 사용자가 앞으로 하는 일

새 기대작은 **노션 작품 DB에만** 넣는다 (네이버 캘린더에 직접 넣지 않는다).
최소 입력: 제목, 종류, 날짜정밀도. 날짜를 알면 출시·개봉일.

날짜를 모를 때가 이 시스템의 핵심이다.
`날짜정밀도 = 미정`, `추정시기 = "2027 상반기"`로 넣으면 캘린더에는 안 뜨지만
대기 목록에 남고, 확정되는 순간 캘린더에 등장하며 알림이 온다.

원제·외부ID·플랫폼·평점·가격은 비워둔다 — 스크립트가 채운다.

---

## 8. 남은 일

### 다음 세션 첫 작업 — 미확인 646건 정리 (사용자 1순위)

**이 프로젝트의 1순위 목적이 여기 있다.** 사용자의 말 그대로 옮기면,
"안 보고 안 하고 넘어간 걸 체크해서 '아 맞다 이런 게 있었지, 이거 해봐야지'"
하는 것이다. 실시간 추적 자동화는 2순위다.

그래서 이 작업은 DB를 채우는 일이 아니다. **사용자가 찍는 "안 봤음" 표시
그 자체가 결과물**이고, 그게 모여 백로그가 된다. 이 점이 방식을 결정한다.

- 기본값을 `완료`로 두고 **안 본 것만 표시**하게 한다.
  13년간 직접 캘린더에 적어둔 목록이라 대부분은 실제로 봤을 것이고,
  무엇보다 표시하는 행위가 곧 백로그를 만드는 일이 된다.
  반대로 하면 본 것을 514번 찍고 나서 백로그를 또 뽑아야 한다.
- 정확도는 덜 중요하다. 완벽한 기록이 아니라 "해봐야지" 후보를 건지는 것이다.
  애매하면 넘어가도 손해가 없다.

현재 분포 (2026-08-18 실측):

| 무리 | 건수 | 처리 |
|---|---|---|
| 스팀 플레이시간 있음 | 131 | 자동 판정 가능. 최소 1시간대이고 1시간 미만은 0건이라 전부 '실제로 함'이 확실. 다만 `완료`인지 `접음`인지는 시간만으로 못 가른다 |
| 소유처만 있음 | 1 | `보유` |
| **아무 근거 없음** | **514** | 영화 237 + 시리즈 33 포함. 나머지는 비스팀 게임이거나 플레이시간 0 |

514건 중 일부는 외부 시청기록으로 자동화할 여지가 있다.
사용자에게 어떤 기록이 있는지 확인할 것 — 왓챠피디아 별점(영화 237건에 가장 강력),
넷플릭스 시청 기록, PSN 트로피(비스팀 게임). **아직 답을 못 들었다.**
자동화되는 만큼 손으로 볼 양이 줄어든다.


### 검증 대기 — 만들었지만 아직 실전에서 안 돌아본 것

2026-08-18 새벽 작업분. GitHub 전면 장애(critical) 중이라 확인이 막혀 있다.
**복구되면 이 다섯 개를 순서대로 확인할 것.**

| # | 확인할 것 | 어떻게 |
|---|---|---|
| 1 | `calendar.yml`이 5분마다 도는가 | 한 번도 안 돌았다. `python healthcheck.py` 또는 Actions 탭 |
| 2 | 예약(cron) 실행이 되는가 | 지금까지 전부 `workflow_dispatch`(수동)였다. 09:00 자동 실행은 미검증 |
| 3 | 예약이 얼마나 밀리는가 | 실행 간격을 몇 번 재볼 것. 실제 지연을 좌우한다 |
| 4 | 클라우드 루틴 첫 실행 | 매일 10시. 루틴 id `trig_01KA1sf3swhxodBx7Rp7A1Ne` |
| 5 | 두 워크플로 동시 push 충돌 | `pull --rebase` 재시도를 넣었으나 실제 충돌은 안 겪어봤다 |

`heartbeat.py`는 매월 1일에만 보내므로 다음 실전은 9월 1일이다.
지금 확인하려면 `python heartbeat.py --force`.

### 사용자 작업

| # | 작업 | 메모 |
|---|---|---|
| 1 | **`ITAD_API_KEY` 시크릿 등록** | 5분. 이게 없어서 매일 가격 추적이 계속 실패 중이다 (`continue-on-error`라 조용히 넘어간다). 등록된 시크릿은 현재 5종이고 ITAD만 빠져 있다 |

https://github.com/jo6417/release-tracker/settings/secrets/actions

### 미구현 기능

| 우선순위 | 작업 | 메모 |
|---|---|---|
| 1 | 게임패스 입점 감지 | 공식 API 없음. **가장 깨지기 쉬운 지점.** 성공 기준 3번이 여기 걸려 있다 |
| 2 | 외부ID 없는 29건 채우기 | 미발표작·정보 부족·DLC 오매칭 거부분 |
| 3 | 후속작 자동 등록 (IGDB 프랜차이즈) | 3차 |
| 4 | 취향 추천 (13년치 675건이 시드) | 3차 |

매칭 스크립트(`match_igdb.py`, `match_tmdb.py`)는 매일 실행에 들어 있지 않다.
채팅으로 노션에 직접 추가한 작품은 외부ID가 빈 채로 남으므로,
가격 추적을 붙이려면 가끔 수동으로 돌려야 한다.

---

## 9. 성공 기준 현황

| # | 기준 | 상태 |
|---|---|---|
| 1 | 미정 작품 날짜 확정 시 하루 안에 알림 | **달성** |
| 2 | 캘린더 수동 수정 0회 | **달성** — 폰 캘린더 구독까지 확인 (2026-08-18) |
| 3 | 게임패스 입점을 찾아보지 않아도 알게 됨 | 미착수 |
| 4 | 할인 알림에 소유 여부 표시 → 중복구매 0회 | **달성** (단 ITAD 시크릿 등록 전까지 실제로는 안 돈다) |
| 5 | 6개월 뒤에도 계속 쓰고 있음 | 시간이 답할 문제. 60일 자동 종료 감시를 두 겹 붙여 대비 |

---

## 10. 작업 시 주의사항

- **실행 전 확인**: 대량 Notion 입력, 파일 삭제, 리포 공개 전환 등
  되돌리기 어려운 작업은 먼저 확인
- **토큰 취급**: 로그·커밋·채팅에 절대 출력 금지
- **한글 우선**: 변수명은 영문이어도 되나 Notion 속성명·알림 문구·작품명은 한글
- **과설계 금지**: 개인용 도구. 사용자 1명. 인증·정산·확장성 불필요
- **60일 자동 종료**: GitHub은 public 리포에 60일간 활동이 없으면 예약
  워크플로를 자동으로 끈다. 워크플로 자신이 만든 커밋은 활동으로 쳐주지
  않는 것으로 알려져 있어, 자동 커밋으로 타이머를 되돌리는 방법은 통하지 않는다.
  사용자는 메일을 거의 보지 않으므로 GitHub의 경고 메일에 기대면 안 된다.
  그래서 감시를 두 겹으로 뒀다.
  - `heartbeat.py` — 매월 1일 노션 멘션으로 생존 신호. 안 오는 달이 있으면 멈춘 것
  - `healthcheck.py` — 워크플로 실행 이력을 밖에서 확인. 매일 10시 예약 작업으로
    자동 실행되며, **이상이 있을 때만** 사용자에게 보고한다
    (예약 작업 id: `release-tracker-healthcheck`)

  **커밋 유무로 판단하지 말 것.** DTSTAMP를 노션 수정시각 기준으로 바꾼 뒤로는
  신작이 없으면 커밋이 안 생기는 게 정상이다. 정확한 신호는 워크플로 실행 이력이고,
  `calendar.yml`은 데이터가 그대로여도 5분마다 돌기 때문에 그게 끊기면 확실한 고장이다.
- **유지보수가 최대 리스크**: 복잡할수록 방치될 확률이 올라간다.
  추가 토큰·추가 계정·수동 갱신이 필요한 방안은 되도록 피한다
- **셸 heredoc에서 백슬래시가 뭉개진다**: 정규식의 `\b` `\n` `\s`나
  윈도우 경로가 든 내용을 heredoc으로 파일에 쓰면 깨진다.
  `chr(92)`로 조립하거나 줄 단위로 패치하거나 raw 문자열을 쓸 것.
  이 프로젝트에서 여러 번 걸렸다
- **오매칭을 의심할 것**: 사용자가 "이거 날짜 이상한데"라고 하면 대체로 맞다.
  고스트리콘이 2001년 원작에 붙은 사례가 있었다 (실제로는 2017 와일드랜드 베타).
  부속 일정보다 출시일이 크게 앞서면 다른 작품에 붙은 신호다
- **알림 설계**: 처음 만들 때 목표가 알림이 107건 한꺼번에 울렸고, 대부분
  "이미 보유중"이었다. 알림은 (1) 미보유 게임만 (2) 이전 값과 비교해
  새로 조건에 들어온 것만 보낸다. 첫 조회는 기준값만 잡는다
