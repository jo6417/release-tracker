# 출시 트래커 — 인계 문서

> 이 파일 하나로 맥락 파악이 되도록 작성됨. 마지막 갱신: 2026-08-20
> 상세 정의는 `PROJECT.md`, 스키마는 `SCHEMA.md`, 사용법은 `README.md`

---

## 0. 한 줄 요약

게임·영화·시리즈의 **출시일을 자동 추적**하는 개인용 시스템.
GitHub Actions가 매일 09:00(KST) API를 조회해 변경분만 Notion에 기록하고,
Notion이 UI·캘린더를, 노션 멘션이 알림을 담당한다.

**서버 없음. 프론트엔드 없음. 앱 없음. 실행 시점에 Claude 없음.**

---

## 1. 사용자

- 조덕근 (부산, 게임 개발자, Unity)
- 감독 역할만 수행. **코딩은 전부 Claude 담당**이 전제
- 한글 UI 선호. 작품명도 한글 우선 (13년간 캘린더에 한글로 기록)
- Notion 상시 사용 중 (식단·운동·체중·구매목록 DB 보유)

---

## 2. 지금 돌아가는 상태 (완료)

### 데이터

| | |
|---|---|
| 작품 DB | **675행** — 게임 399 / 영화 241 / 시리즈 35 |
| 일정 DB | 29행 (베타·데모·DLC 등 부속 사건 + 최종화 1) |
| 외부ID | 646건 (igdb 377 + tmdb 269) |
| 소유·플레이 | 소유처 145건, 플레이시간 139건 (총 4,765시간) |
| 가격 | 194건 (정가·현재최저가·역대최저가) — **전부 스팀 기준.** 2026-08-22부터 `현재최저가`·`역대최저가`는 전 상점 최저가 아니라 스팀 가격만 본다. 사용자가 스팀에서만 사기 때문에 Fanatical·GMG 같은 키 리셀러 특가는 잡지 않는다. 스팀에 안 파는 게임은 값을 안 채우고 조용히 넘어간다 (2026-08-22 기준 316건 중 27건). 구현은 `adapters/itad.py`의 `prices()`(`shops=61`)와 `history_low()`(`storelow/v2` — `historylow/v1`은 shops를 무시해서 못 쓴다) |
| 진행도(게임) | 졸업 201 / 구매 보류 51 / 구매 대기 48 / 보유함 34 / 폐기됨(노잼) 25 / 일시 중단 20 / 출시 대기 11 / 엔딩 없음 7 / 진행 중 3 (2026-08-19) |
| 진행도(영상) | 시청 완료 206 / 시청 안함 61 / 공개 대기 6 / 미확인 2 / 시청 보류 1 |

### 매일 09:00 자동 실행 (`.github/workflows/daily.yml`)

```
스팀 동기화 → 가격 추적(ITAD) → 출시일 재조회(IGDB·TMDB)
  → 시리즈 완결 추적(TMDB) → 소개 채우기 → 새 트레일러 감지
  → 변화 감지·전환 → 알림 → 스냅샷 커밋
```

각 단계는 `continue-on-error`로 격리. 한 소스가 죽어도 추적은 계속된다.
2026-08-17에 수동 실행으로 전 과정 검증 완료 (초록불).

`.ics` 생성은 여기 없다. `calendar.yml`이 따로 자주 돌린다(3절).
예약은 UTC 00:00(09:00 KST)이지만 실제 시작은 00:41~00:44Z로 40분쯤
밀린다 (2026-08-18~21 4회 모두).

### 출시일 재조회 (`refresh_dates.py`, 2026-08-22)

`track.py`는 노션의 변화만 본다. 바깥 세상의 발표를 노션에 실어주는 단계가
없어서, `[날짜확정]` 알림은 사람이 직접 노션을 고쳤을 때만 울렸다.
`sync_series.py`가 방영 시작 **이후**(완결)를 맡는다면, 이쪽은 그 앞 구간 —
**아직 안 나온 것의 출시일**을 맡는다. `track.py` 앞에 있어야 같은 날
알림으로 이어진다.

규칙 (좁게 잡았다):

- 날짜정밀도가 `확정`·`완결대기`인 행은 **보지 않는다.** 사람이 정한 값을
  소스가 덮어쓰지 않게. 대가로 **출시 연기는 감지하지 못한다** — 필요해지면
  '확정인데 소스와 다름'을 값 변경 없이 알림으로만 내보내는 방식을 얹을 것
- 정밀도가 **올라갈 때만** 쓴다 (미정 → 연도 → 분기 → 월 → 확정)
- `월`·`분기`·`연도`면 출시·개봉일에 **구간의 첫날**을 넣는다.
  `track.py`의 `released()`가 "확정이 아닌 날짜는 자리표시자"로 읽는 규칙과
  같은 약속이다
- 영화는 국내 개봉일을 알게 되면 그걸로 덮어쓴다
- 바뀌는 값이 없으면 노션을 건드리지 않는다 (수정시각이 바뀌면 .ics가 매번
  새로 만들어진다)

정밀도를 알아내는 법:

- IGDB — `release_dates.category` (0=일자, 1=월, 2=연도, 3~6=분기, 7=미정).
  `first_release_date`만 보면 '2026년 언젠가'도 2026-12-31로 내려와서 확정과
  구분되지 않는다. 필드명이 바뀐 API를 만나면 `human` 문자열("Q4 2026")로
  판단하는 경로가 따로 있다
- TMDB — 정밀도 개념이 없다. `status`가 Planned/Rumored이고 날짜가 01-01이면
  연도만 아는 자리표시로 보고 '연도'로 낮춘다. 그 외에 날짜가 있으면 확정.
  **이 휴리스틱이 가장 약한 고리다** — 잘못된 `[날짜확정]`이 나오면 여기부터
  볼 것

바꾸지 않고 확인만 하려면 Actions 탭의 **"출시일 재조회 점검 (--dry)"**
워크플로를 수동 실행한다 (`refresh_check.yml`).

### 알림 (2026-08-19 카드로 개편)

노션에 사용자를 @멘션 → 폰 푸시. **검증 완료 (즉시 도착 확인).**
텔레그램은 쓰지 않는다.

**알림 1건 = 알림 DB의 카드 한 장이다.** 예전에는 페이지 한 장에 문단을 계속
쌓았는데, 쌓일수록 어제 것과 오늘 것이 뒤엉켜 읽을 수가 없었다. 카드로 나누면
날짜·종류로 거르고 읽은 건 `확인함`으로 지울 수 있다.

카드 본문은 항상 같은 순서다 (`notify.send_card`):

```
@멘션 + 카드 제목          ← 이 줄이 폰 푸시를 띄운다
요약     [신규] 랜턴스, 더 펭귄     ← 제목만. 넘길지 열지 여기서 정한다
상세     [신규] 랜턴스
           시리즈 · 쿠팡플레이 · 방영중
           2026-08-18 공개 · 1/8화 · 다음 08-25 · 완결예정 10-06
           → 지금 주간 방영 중. 몰아볼 거면 완결까지 대기
```

상세 문구는 `describe.py`가 만든다. **마지막 줄은 항상 "그래서 언제부터 보면
되나"다.** 제목만 뜨는 알림은 "이게 뭐였지"로 끝나서 행동으로 이어지지 않는다.

- **카드 제목은 항상 날짜로 시작한다** — `2026-08-19 (수) · 오늘의 변경 4건`.
  날짜만 쓰면 하루에 여러 장(변경·할인·브리핑) 나올 때 제목이 전부 같아지므로
  뒤에 라벨이 붙는다. 이 조립은 `notify.card_title()` 한 곳에서만 한다.
- 하루 한 장. 급한 게 있으면 제목이 `지금 확인 N건 · 그 외 변경 M건`이 되고
  상세도 급한 것부터 온다 (급함: 날짜확정, 게임패스 입점, 볼차례,
  **완결대기 작품의 완결**)
- 그 외: 날짜 변경, 진행도 변경, 신규, 대기 표시 없는 완결·취소
- 할인 판정은 `sync_prices.py`가 한다 (세일 기간에 낱개로 쏟아지지 않게 묶어서).
  발송은 아침 카드에 합쳐진다 — 아래 `하루에 카드 두 장` 참조.
  **대상은 `진행도(게임)`이 `구매 대기`·`출시 대기`인 것만** (`sync_prices.WANT`).
  소유처가 비었는지로 판정하던 것을 2026-08-21에 바꿨다 — 아래 참조
- 변경 없는 날은 아무 알림도 보내지 않는다
- `notify_ids.json`에 `notify_db`가 없으면 예전 방식(페이지에 줄 추가)으로
  자동으로 떨어진다. DB를 지워도 알림이 끊기지는 않는다.

### 하루에 카드 두 장 — 아침 하나, 저녁 하나 (2026-08-22)

카드를 만드는 스크립트가 넷이다(`track`·`sync_prices`·`sync_media`·`briefing`,
월 1회 `heartbeat`). 각자 자기 일을 끝내면서 자기 카드를 보내니 **아침 3장,
저녁 2장**이 따로 왔다(2026-08-21 실측 5장). 각 스크립트 안에서 N건을 1장으로
묶는 것까지는 예전에 했지만, 스크립트끼리는 서로를 몰라서 묶을 자리가 없었다.

`NOTIFY_SPOOL` 환경변수에 파일 경로가 있으면 `notify.send_card()`는 보내지 않고
그 파일에 카드를 적어둔다. 워크플로 마지막 스텝의 `python notify.py --flush`가
모아서 한 장으로 보낸다. **환경변수가 없으면(로컬 실행) 예전처럼 그때그때
보낸다** — 로컬에서 스크립트 하나만 돌려볼 때 알림이 안 오면 헷갈린다.

합칠 때 부분 순서는 `notify.PART_RANK`가 정한다. 저녁은 브리핑이 본편이라 맨
앞, 아침은 "무슨 일이 있었나"(변경)가 먼저, 할인·영상은 훑어보는 것이라 뒤.

함정 셋:

- **발송 스텝은 `if: always()`** — 앞 스텝 하나가 죽어도 나머지는 알려야 한다
- **보낸 뒤 스풀 파일을 지운다** — 남기면 다음 실행에서 같은 알림이 또 나간다.
  `.gitignore`에도 넣었다(러너마다 새로 시작해야 하는 파일이다)
- **모아둔 게 없으면 조용히 끝낸다**(exit 0). `if: always()` 스텝이 여기서
  실패하면 변경 없는 날마다 워크플로가 빨갛게 뜬다

블록 예산은 여유가 있다. 현실적인 저녁 합본(브리핑 12건 + 영상 2건)이 74블록,
상한은 95다. 넘치면 `요약`은 온전히 남고 `상세` 뒤쪽이 잘린다
(`… 나머지는 작품 DB에서 확인`).

### 캘린더에 같은 작품이 두 줄로 뜨던 것 (2026-08-22)

**대표 사건(정식출시·극장개봉·국내개봉·OTT공개·시즌시작)은 작품 행에 흡수하고,
부속 사건(베타·데모·DLC·최종화)만 일정 행으로 남긴다.** 이관할 때 `migrate.py`가
지킨 규칙인데(`대표출시일` 흡수), 그 뒤에 작품 행과 일정 행을 둘 다 만들면
아무도 막지 않았다. `make_ics.py`는 두 루프를 독립적으로 돌기 때문에 그대로
같은 날 같은 제목이 두 줄로 나갔다 (2026-09-04 귀무자, 2026-09-16 애니모).

이제 일정 루프가 `emitted`(작품 루프가 실제로 내보낸 `(작품 id, 시작일)`)와
대조해서 겹치면 건너뛴다. 51 → 49건.

**판정 기준이 "작품 DB에 확정 날짜가 있는가"면 안 된다.** 작품 루프는 확정
날짜가 있어도 이벤트를 안 내는 경우가 셋 있다 — 6개월 컷오프 이전,
`종류=시리즈`(`SKIP_WORK_KINDS`), `--limit`. 그때 일정 행까지 빼면 그 날짜가
캘린더에서 통째로 사라진다. 실제로 더 펭귄(OTT공개)과 랜턴스(시즌시작)가 둘 다
`종류=시리즈`다. **실제로 내보낸 것만** 기준으로 삼으면 셋 다 저절로 걸러진다.

### 알림 카드의 출처 줄 (2026-08-22)

`[신규]`에는 `등록: 누구 · 날짜`, `[상태변경]`·`[날짜변경]`·`[전환]`·`[취소]`에는
`수정: 누구 · 날짜`가 붙는다. 나중에 "이거 왜 이래?"를 되짚는 데 쓴다 — 위
캘린더 중복을 추적할 때 스크립트가 넣은 것인지 아닌지가 갈림길이었다.

값은 노션 속성이 아니라 페이지의 `created_by`/`last_edited_time`이라
`snapshot()`이 직접 담는다(`EXTRA`에 적으면 안 된다). 이름은 `/v1/users`를
한 번 읽어서 붙인다. 워크플로가 쓰는 통합 토큰은 `출시 트래커`로 찍힌다.

**한계: 노션은 MCP로 넣은 것도 사람 계정으로 찍는다.** "스크립트냐 아니냐"는
갈라주지만 "손으로 넣었냐 에이전트가 넣었냐"는 못 가른다. 2026-08-21에 이
값을 근거로 "사용자가 직접 만든 행"이라고 판단했다가 틀렸다.

옛 스냅샷에는 이 값이 없어서 출처 줄이 그냥 빠진다(터지지 않는다).

### 진행도를 자동으로 바꾸는 규칙은 둘뿐이다 (2026-08-21)

**진행도는 사람이 정하는 값이다.** 자동으로 쓰는 곳은 이 둘만 남았다.

```
sync_steam.py       플레이 0시간 + 현재값이 '미확인'  → 보유함
track.promote_stage 확정 출시일 경과 + 출시 대기      → 구매 대기 (아래 절)
```

`add_steam_games.py`가 새로 만드는 행도 진행도는 무조건 `미확인`이다.

**뺀 규칙 — "최근 90일 안에 플레이 기록이 있으면 진행 중".** 플레이 기록은
과거형인데 `진행 중`은 현재형이라 둘은 같은 말이 아니다. 253시간을 하고 졸업한
사람과 지금 붙잡고 있는 사람이 스팀 API에서는 구분되지 않는다. 이 규칙은 현재
값을 보지 않고 덮어썼고 **매일 재적용됐다.** 2026-08-19에 사람이 찍은 6건
(프래그마타·레지던트 이블: 레퀴엠·붉은사막·숲속의 작은 마녀 → 졸업,
Untitled Goose Game → 일시 중단, MECCHA CHAMELEON → 보유함)이 다음 날 아침
9시에 전부 `진행 중`으로 되돌아왔다. 변경이력만 남고 값은 사라졌다.

보호 집합이나 `수동고정` 체크박스로 감싸는 안을 검토했으나, 전제가 틀린 규칙을
예외로 덧대는 것이라 규칙 자체를 뺐다. 플레이시간·마지막플레이일은 그대로
기록되므로 판단 재료는 잃지 않는다.

**곁가지로 `방치` 알림이 같이 사라졌다.** 판정 대상이 `진행 중`인 행이었고 그
대부분이 이 규칙이 만든 값이었다.

### 출시일이 지나면 대기를 자동으로 푼다 (2026-08-20)

`출시 대기`는 "아직 안 나왔다"는 뜻이다. 그런데 나온 뒤에도 그대로 두면 기대작
뷰에 계속 눌러앉고 백로그로는 안 넘어간다. 날짜가 이미 말해주고 있는 걸 사람이
손으로 옮기고 있었다. `track.promote_stage()`가 매일 대신 옮긴다.

```
진행도(게임)   출시 대기 → 구매 대기
진행도(영상)   공개 대기 → 시청 안함
```

**추정 날짜로는 안 옮긴다.** 날짜정밀도가 `월`·`분기`면 출시·개봉일은 그 구간의
첫날이 든 자리표시자다. 그걸 지났다고 대기를 풀면 아직 안 나온 게임이 구매
대기로 올라간다. `확정`만 근거로 쓴다. `완결대기`는 완결일이 추정치일 수 있어
날짜가 아니라 **방영상태가 `완결`/`시즌완결`인지**를 본다 (`track.released()`).

순서에 함정이 둘 있다.

- **대조가 끝난 뒤에 옮긴다.** 먼저 옮기면 우리가 쓴 값이 그대로 `[상태변경]`
  알림이 된다 — 자기가 한 일을 자기가 알리는 꼴이다
- **스냅샷에도 옮긴 값을 담는다.** 옛 값이 남으면 내일 대조에서 되살아난다

오늘 나온 작품은 `[전환]`으로 세지 않는다. `available_check()`가 이미
`[오늘 공개]`로 알리고 있어서 카드에 같은 작품이 두 줄로 뜬다.

### 작품 소개 — 알림에 "이게 뭐였지"를 없애는 한 줄

추천 칸은 몇 년 전에 적어둔 작품이 올라오는 자리라 제목만으로는 아무 판단도
안 된다. `소개` 속성(rich_text)을 두고 `sync_intro.py`가 채운다.
**한국어가 있는 소스를 먼저 본다.**

```
게임        스팀 short_description (한국어)  →  IGDB summary (영문)
영화·시리즈   TMDB overview (ko-KR)          →  TMDB overview (영문)
```

한 번 채우면 다시 안 건드린다(`--force` 예외). 매일 `--limit 120`으로 밀기
때문에 새로 들어온 작품은 알아서 붙는다. 알림에 넣을 때 자르는 건
`describe.intro_line()`이고, 문장 중간이 아니라 마침표에서 끊는다.

**2026-08-20 전수 백필 완료 — 682행 중 654행.** 소스별로 스팀 143 / TMDB 275 /
IGDB 236이다. 못 채운 28건은 전부 근거가 없는 쪽이다 — 27건은 외부ID 자체가
없고(미구현 2번과 같은 목록), 1건은 `Dead Island Riptide`인데 스팀에서 상장
폐지돼 어느 지역으로 물어도 `success: false`가 온다.

백필하다 발견해 고친 것: **`더 펭귄`·`랜턴스`의 외부ID가 `tmdb:194764`처럼
media_type이 빠진 옛 형식**이었다. 이관 시절 흔적이고 지금 `apply_tmdb.py`는
`tmdb:tv:194764`로 제대로 쓴다. 스크립트마다 폴백을 넣는 대신 **노션의 두 행을
정식 형식으로 고쳤다** — 소비하는 쪽이 셋(`sync_intro`·`sync_series`·`sync_media`)
이라 데이터를 고치는 게 싸다. 덤으로 `sync_series.py`가 그동안 이 둘을
`ID없음`으로 건너뛰던 것도 같이 풀렸다.

`describe.INTRO_KINDS`가 붙일 자리를 정한다 — 추천·오늘 공개·곧·신규·영상처럼
"이게 뭐였지"가 실제로 문제가 되는 칸만이다. 진행 중·방치는 지금 붙잡고 있는
것이라 설명이 필요 없다.

### 새 트레일러 감지 (2026-08-20 추가) — `sync_media.py`

**계기.** 2026-08-20 오전 11시(KST)에 블랙 미스: 종규의 15분 게임플레이
트레일러가 올라왔는데 그날 저녁 브리핑에 한 줄도 없었다. 이유가 둘이었다.

1. 영상을 보는 코드가 아예 없었다
2. `블랙 미스: 종규`가 작품 DB에 없었다 — **있어도 못 잡았을 상황이다**

**소스를 넷 쓰는 이유는 하나다 — 속도.** 같은 트레일러라도 어디서 보느냐에
따라 며칠이 갈린다.

| 소스 | 지연 | 대상 | 키 |
|---|---|---|---|
| **유튜브 채널 RSS** | 몇 분 | 채널을 등록해둔 작품 | 불필요 |
| 스팀 `movies` | 같은 날 | 스팀 상점 페이지가 있는 게임 | 불필요 |
| TMDB `/videos` | 같은 날~하루 | 영화·시리즈 | TMDB |
| IGDB `videos` | **몇 주** | 나머지 게임 | TWITCH |

종규가 딱 이 차이에 걸렸다. 그날 IGDB의 마지막 갱신은 **7월 21일**이었고 스팀
페이지는 아직 없다. **유튜브 RSS만 그날 안에 알 수 있었다.** 그래서 채널
등록이 이 스크립트에서 제일 값나가는 부분이다.

```
python sync_media.py --add-channel @BlackMythGame "블랙 미스: 종규"
```

핸들을 주면 채널 id를 알아서 찾아 `youtube_watch.json`에 적는다. 제목으로 더
좁히려면 그 항목의 `키워드`에 단어를 넣는다 (퍼블리셔 채널처럼 여러 작품을
올리는 곳에 필요하다). **채널 감시는 작품 DB에 없는 것도 볼 수 있는 유일한
경로다** — 후속작이 처음 발표되는 순간이 여기로 들어온다.

- 대상은 **진행도가 대기 계열이고 나온 지 120일 안쪽**인 작품이다
  (`WAITING`, `STALE_DAYS`). 이미 한 게임의 트레일러는 소식이 아니다
- **첫 실행은 조용하다.** 작품 하나에 트레일러가 열몇 개씩 달려 있어서 처음부터
  알리면 수백 건이 터진다. 처음 본 작품은 기준값만 잡는다 (`sync_prices.py`와 같다)
- 최신 영상 주소는 `레퍼런스` 속성에 남는다. 노션에서 바로 눌러 볼 수 있다
- **아침·저녁 두 번 돈다.** 트레일러는 낮에 뜬다. 09:00에만 보면 그날 저녁
  브리핑에 한 줄도 없이 지나간다 — 실제로 그렇게 놓쳤다
- `media_state.json`을 워크플로가 커밋한다. **안 하면 러너가 매번 빈 상태로
  시작해 영원히 "첫 실행"이 되고, 새 영상을 한 번도 못 잡는다**

### 저녁 브리핑 19:00 KST (`.github/workflows/evening.yml`)

아침 `track.py`는 **어제와 달라진 것**만 알린다. 변한 게 없으면 조용하고,
그래서 "오늘 뭐 볼까"에는 답을 못 한다. `briefing.py`는 반대로 **지금 상태**를
매일 같은 시각에 늘어놓는다. 순서에 의도가 있다 — 이 프로젝트의 1순위가
"놓치고 넘어간 걸 발견하는 것"이라 아래로 갈수록 그쪽에 가깝다.

```
오늘 공개    이용 가능일이 오늘인 것
7일 내 공개  이용 가능일이 D+1~D+7
완결 임박    완결대기 시리즈 중 완결일 D+0~D+14
진행 중      진행 중·시청 중 전부 (마지막 플레이 최신순)
오늘의 추천   이미 나왔는데 아직 안 한 것 중 3건
```

마지막 칸은 날짜를 커서 삼아 매일 3칸씩 밀린다(`pick_backlog`). 무작위면
재현이 안 되고, 앞에서 자르면 같은 작품이 영원히 1번이라서 그렇다.

**라벨은 구어체를 쓰지 않는다** (2026-08-20). 원래 마지막 칸이 `아 맞다 이거`,
첫 칸이 `오늘부터`였다. 한 번은 재밌지만 매일 같은 자리에 뜨면 실없어진다.
라벨은 무엇을 모아둔 칸인지만 적고, 톤은 카드 전체에서 일관되게 간다.
(1순위 목적을 적을 때 쓰던 사용자의 표현 "아 맞다 이런 게 있었지"는 그대로
살아 있다 — 그건 **왜 만드는지**이지 **화면에 뭐라고 쓸지**가 아니었다.)

**붙잡고 있는 게 있으면 추천을 3건에서 1건으로 줄인다** (`BACKLOG_PICK_BUSY`).
할 게 밀려 있는데 매일 새 작품을 셋씩 들이미는 건 도움이 아니라 소음이다.
판정은 `busy()` — 진행 중인 행 가운데 **최근 14일 안에 실제로 손댄 기록**
(`마지막플레이일`)이 있는지만 본다.

여기에 한계가 있고 알고 쓰는 것이다. **"얼마나 봤는지"는 게임만 알 수 있다.**
스팀이 마지막플레이일을 주기 때문이다. 드라마를 3화까지 봤는지 8화까지 봤는지는
아무 데도 안 적혀 있어서, 영상물만 붙잡고 있는 날은 추천이 그대로 3건 나온다.
증거가 있는 행만 세는 쪽으로 정했다 — 넘겨짚어 추천을 끄면 어느 날 조용해진
이유를 알 수 없게 된다. 시청 진행을 손으로라도 적기 시작하면 그때 붙이면 된다.

이 워크플로는 노션을 읽기만 하므로 커밋이 없다.

### 노션 select 옵션 이름 바꾸기 — 손으로 할 것

**API로 하지 말 것.** 노션 API는 select 옵션 이름 변경을 지원하지 않는다.
id와 새 이름을 같이 보내도 조용히 무시된다. 더 나쁜 건 옵션 목록이 **전체 교체**
방식이라, 바꿀 것만 담아 보내면 나머지 옵션이 전부 삭제되고 그 값을 쓰던 행이
API에서 빈 값으로 보인다. 2026-08-18과 2026-08-19에 같은 사고를 두 번 냈다.

**노션 UI에는 이름 변경이 있다.** 옵션 옆 `⋯` → 이름 수정, 두 번 클릭이면 전 행에
반영된다. 스크립트로 200행을 다시 찍는 것보다 비교도 안 되게 빠르다.
**사람에게 부탁하는 게 정답이다.**

정말 코드로 해야 하면 `rename_option.py`를 쓴다. 순서가 정해져 있다 —
(1) 새 옵션 추가 (기존 전체 + 새것) → (2) 행을 전부 새 값으로 다시 찍기 →
(3) 옛 옵션만 뺀 전체 목록 보내기. 이 순서를 어기면 위 사고가 재현된다.

값이 이미 날아갔다면: **지워진 게 아니라 옵션이 없어서 안 보이는 것뿐이다.**
같은 이름으로 옵션을 되살리면 그대로 돌아온다 (`restore_stage.py`가 1차 복구본).

### 캘린더 아이콘

`.ics` 제목 맨 앞에 카테고리 아이콘 한 글자를 붙인다(`make_ics.py`).
`[게임]` 같은 말머리는 자리를 많이 먹어 캘린더 격자에서 제목이 잘렸다.

```
🎮 게임      게임 작품 + 게임 일정 전부 (발표·예약구매·테스트·데모·
             얼리액세스·정식출시·DLC·무료배포)
🍿 영화      영화 작품 + 극장개봉·국내개봉
📺 시리즈    시리즈 작품 + OTT공개·시즌시작·파트공개
📗 만화·도서  만화 작품 + 회차   (`도서`는 아직 종류에 없다. 생기면 그대로 붙는다)
🏁 완결      최종화 — "이제 정주행 가능"이라 이것만 따로 뺐다
```

**늘리지 말 것. 다섯 개가 상한이다.** 종류마다 다른 그림을 주면 20개가 되고,
그러면 아무도 못 외워서 아이콘이 그냥 장식이 된다. 예약구매인지 DLC인지는
일정 이름과 DESCRIPTION에 이미 글자로 들어 있다.

**고를 때는 주 색깔이 겹치는지를 본다.** 격자에서 14px로 줄면 그림이 아니라
색덩어리로 읽힌다. 처음에 🎬·📺·🎮로 갔다가 셋 다 어두운 회색이라 구별이 안 돼
영화를 🍿(빨강), 만화를 📗(초록)로 바꿨다.
지금 배치는 회색 · 빨강 · 갈색 · 초록 · 흑백으로 한 번도 안 겹친다.

`애니메이션` 체크는 아이콘으로 나누지 않았다. 서양 애니·애니 극장판까지 묶는
축이라 흔히 쓰는 🍥가 절반은 틀린 그림이 된다.

### 노션 뷰 6개 (작품 DB)

출시 캘린더 / 기대작 / 미확인 정리 / 내 라이브러리 / 백로그 / 플레이 중
(뷰 이름은 노션에서 만든 그대로다. 속성 이름이 바뀌었으니 필터도 손봐야 한다)
열 순서: 제목 · 출시·개봉일 · 날짜정밀도 · 종류 · 플랫폼 · 공개처 · 소유처 · 플레이시간 · 마지막플레이일

주의: 노션 API는 뷰를 만들 수 없다. 뷰 작업은 브라우저 자동화로 했고,
툴바 팝업이 자동 클릭에 닫히는 문제가 있어 **열 머리글 메뉴**와
**뷰 탭 메뉴**, **표 우측 `+` 옆 `⋯`** 를 우회로로 써야 한다.

---

## 3. .ics 캘린더 구독 — 해결됨 (2026-08-18)

**네이버 캘린더에 정상 표시 확인.**

### 원인은 하나였다

`CR CR LF` 이중 변환. 파일을 줄바꿈 자동변환 모드로 열고 내용에도 CRLF를
붙여서 388줄 전부 줄 끝에 여분의 CR이 달렸다. iCalendar 파서가 파일 전체를
거부해서 구독은 등록되는데 일정이 0건이었다. 커밋 `2d65f21`에서 해결.

### 틀렸던 두 번째 가설

"git이 CRLF를 LF로 바꿔서 표준 위반이 된다"고 보고 `.gitattributes`에
`*.ics -text`를 넣었는데, **그 커밋은 배포되지도 않았다.** 지금 네이버가
정상적으로 읽고 있는 배포본은 LF다. 즉 네이버는 LF를 문제없이 받아들인다.
그래서 `*.ics text eol=lf`로 뒤집어, 실제 동작이 확인된 형식을 고정했다
(커밋 `1189a28`). 줄바꿈 종류는 원인이 아니었다.

### 반영 지연 — 측정 완료 (2026-08-18, 2026-08-22 갱신)

노션에 넣은 작품이 폰 캘린더에 뜨기까지의 각 구간을 실측했다.

| 구간 | 실측값 |
|---|---|
| 노션 수정 → `calendar.yml`이 집어감 | **평균 16분, 최악 103분** (다음 항) |
| 워크플로 실행 자체 | **11~16초** |
| push → GitHub Pages 배포 완료 | **67초** |
| 배포 → 네이버 캘린더 표시 | **3분 18초 이내** (00:58:42 배포 → 01:02 확인) |
| **합계** | **보통 20분 안팎, 최악 1시간 45분** |
| 참고: 1차 측정 | 13분 38초 이내 (00:38:22 → 01:52 사이 확인) |

**결론: 네이버는 병목이 아니다.** 배포만 끝나면 몇 분 안에 반영된다.
남은 병목은 **다음 예약 차례를 기다리는 시간** 하나뿐이다. 일하는 시간이
아니라 그냥 기다리는 시간이다 — 실행 자체는 15초면 끝난다.

즉시 반영이 필요하면 예약을 건너뛰고 직접 돌린다. 하는 일은 완전히 같고,
배포까지 1분 20초 · 폰에 뜨기까지 4~5분이다.

```
gh workflow run calendar.yml --repo jo6417/release-tracker
```

Actions 탭의 `캘린더 갱신` → `Run workflow` 버튼도 같은 동작이라 폰에서도
누를 수 있다.

### `cron: */5`는 5분마다가 아니다 (2026-08-22 실측)

예약 실행 100건(2026-08-19 17:36Z ~ 08-21 23:59Z)의 간격을 전부 쟀다.

| | 값 |
|---|---|
| 최소 | 13분 |
| 중앙값 | 29분 |
| 평균 | 33분 |
| 최대 | **103분** |
| 10분 이하였던 횟수 | **0회** |
| 하루 실제 실행 | **44회** (예약대로면 288회) |

**약속의 15%만 돌아간다.** `*/5`를 적어놨다고 5분마다 도는 게 아니다.
GitHub 예약은 보장이 아니라 여유 있을 때 돌려주는 것이고, 공식 최소
간격도 5분이라 `*/1`로 낮춰봐야 얻는 게 없다. daily.yml도 같은 이유로
매일 40분씩 밀린다.

주기 숫자를 만져서 개선할 수 있는 구간이 아니다. 그래도 비용이 0이라
2026-08-22에 `* * * * *`("가능한 한 자주")로 올려뒀다. 실행 횟수가 늘지
않아도 잃는 게 없고, GitHub에 여유가 생기면 그만큼 가져가기 때문이다.
**기대는 하지 말 것 — 이건 개선이 아니라 공짜라서 열어둔 것이다.**

실질적인 해법은 둘뿐이다.

1. **에이전트가 노션을 고쳤으면 그 자리에서 수동으로 건다.** 2026-08-22
   도입. 규칙은 리포 루트 `CLAUDE.md`에 있다. 사용자가 "이거 등록해줘"라고
   해서 세션이 DB를 건드린 경우가 즉시성이 가장 아쉬운 상황인데, 마침
   그때는 에이전트가 붙어 있으니 직접 누르면 된다. 4~5분이면 폰에 뜬다.
2. **노션 웹훅 → `repository_dispatch`** — 사용자가 손으로 고친 것까지
   잡으려면 이 방법이라야 한다. 미검토. 노션 자동화의 웹훅 액션이
   현재 플랜에서 되는지부터 확인해야 한다.

비용은 어느 쪽도 문제가 아니다 — 리포가 public이라 Actions는 0원이고,
노션 API도 실행당 10건 남짓이라 한도에 한참 못 미친다.

### 예약을 올린 뒤 확인할 것

`* * * * *`가 유효하지 않은 값으로 취급돼 **예약이 통째로 무시될** 가능성이
있다. 그러면 캘린더가 조용히 멈춘다. 바꾼 뒤 몇 시간 안에 `schedule` 이벤트
실행이 계속 잡히는지 볼 것:

```
gh run list --workflow=calendar.yml --limit 10 --repo jo6417/release-tracker
```

`healthcheck.py`도 3시간 침묵하면 잡아준다(기준을 2→3시간으로 올렸다.
실측 최대 간격 103분과 겹쳐 오경보가 나던 값이었다).

### 주기를 올리기 전에 반드시 고칠 것

~~`cron`을 매시로 바꾸면 최대 지연이 24시간에서 1시간대로 줄어든다~~ →
**2026-08-18 `calendar.yml` 분리로 해결.** 아래는 그때 함께 정리한 것들이다.

~~`track.py`의 `idle_check()`가 같은 방치를 매일 다시 알린다~~ →
**2026-08-21 해결. 방치 알림 자체를 뺐다** (`idle_check`·`notify_state.json`
모두 삭제). 지금 하고 있는 게임 옆에 다른 게임을 들이미는 알림이라 손이 가지
않았고, 판정 대상인 `진행 중`의 대부분이 사람이 찍은 값이 아니라 sync_steam이
플레이 기록으로 자동으로 넣던 값이었다. 안 한 게 뭔지는 작품 DB를 직접 보면
되고, 백로그 추천이 그 자리를 맡는다.

한편 `.ics` 배포 경로를 raw.githubusercontent나 서버리스로 바꾸는 안은
**실익이 없다.** 배포 구간은 67초라 병목이 아니다. 2026-08-17의 5연속 실패는
GitHub 전면 장애(critical)라는 예외 상황이었다.

### 막고 있는 것 — GitHub 장애 (우리 코드 문제 아님)

2026-08-17 13:40Z 시작된 GitHub 전면 장애로 **Pages 배포가 3연속 실패**했다.

```
Failed to create deployment (status: 503) ... is githubstatus.com reporting a Pages outage?
```

장애 복구 후 아무 커밋이나 하나 push하면 배포가 재시도된다.
그때까지 배포본은 `2d65f21` 시점(43건)에 멈춰 있다 — 내용상 최신과
줄바꿈 빼고 동일해서 지금 당장의 피해는 없다.

**주의: 이 장애는 매일 09:00 자동 실행(Actions)도 막는다.**
복구 후 워크플로가 정상 도는지 한 번 확인할 것.

## 4. 자격 증명

`.env`(gitignore됨)에 6종. GitHub 시크릿에도 동일하게 필요.

| 키 | 로컬 .env | GitHub 시크릿 |
|---|---|---|
| `NOTION_TOKEN` | O | O |
| `TMDB_API_KEY` | O | O |
| `TWITCH_CLIENT_ID` / `_SECRET` | O | O |
| `STEAM_API_KEY` | O | O |
| `ITAD_API_KEY` | O | **X — 등록 필요** |

**`ITAD_API_KEY`를 GitHub 시크릿에 넣어야 매일 가격 추적이 돈다.**
https://github.com/jo6417/release-tracker/settings/secrets/actions

GitHub 시크릿은 쓰기 전용이라 값을 다시 볼 수 없다. 로컬 `.env`가 사본이다.
토큰은 채팅·로그·커밋에 절대 출력하지 말 것.

### 상수

```
Notion 부모 페이지: 3be857fe-7a80-808e-b6ad-c595ff14a3e8   (출시 트래커)
작품 DB:   3be857fe-7a80-81cb-8c69-fc96da6418f1
일정 DB:   3be857fe-7a80-8177-a7b5-ee73eb4de331
알림 DB:   3c1857fe-7a80-81bb-97aa-c87e017e1754   (카드. 현행)
알림 페이지: 3bf857fe-7a80-810d-bc54-dfc8f08aac26   (구방식 폴백용)
GitHub:   jo6417/release-tracker  (public — Pages 때문에 전환)
작업 폴더:  C:\Users\Javis.JAVIS\Documents\GitHub\release-tracker
캘린더 원본: C:\Users\Javis.JAVIS\Downloads\Calendar_ejrrms22_2026-08-04-232847.ics
SteamID:  76561197996869791 (vanity: jo6417)
```

### 상태 파일 (워크플로가 커밋한다 — 안 하면 기능이 죽는다)

| 파일 | 무엇을 기억하나 | 안 커밋하면 |
|---|---|---|
| `snapshots/*.json` | 어제의 작품 DB 전체 | 매일 전 행이 신규로 뜬다 |
| `media_state.json` | 어떤 트레일러를 이미 알렸나 | **영원히 첫 실행이라 새 영상을 한 번도 못 잡는다** |
| `youtube_watch.json` | 감시할 유튜브 채널 (사람이 등록) | 등록이 날아간다 |

`gh` CLI 인증됨 → Claude가 커밋·push 가능.
단 시스템 credential.helper가 간섭해 GitHub 로그인 GUI 창이 뜨는 일이 있다.
**`GIT_TERMINAL_PROMPT=0`을 붙여 실행**하고, 창이 떴다면 사용자에게 취소를
안내한 뒤 `gh auth setup-git`을 재실행하면 된다. (비밀번호 인증은 폐지됐으니
그 창에 입력해도 실패한다.)

---

## 5. 구조 (원안에서 바뀐 것)

원안은 모든 날짜를 일정 DB 행으로 만들었으나, 97%가 일정 1개짜리라
같은 날짜를 작품과 일정 두 곳에 쓰는 중복이 생겼다. 그래서:

- **메인 날짜(정식출시·극장개봉·OTT공개·시즌시작)는 작품 행이 직접 보유**
- 부속 사건(베타·데모·DLC·예약구매)만 일정 행 → 669행에서 28행으로
- 순차 공개물은 `출시·개봉일`을 기간으로 (쉬헐크 2022-08-17 ~ 10-13)
- 종류를 `게임/영화/시리즈/만화` 4종 + `애니메이션` 체크박스로 (조합 폭발 방지)
- 과거 기록은 `완료`가 아니라 **`미확인`** (실제로 했는지 모르므로)
- **안 쓰는 속성 7개 정리** (2026-08-18) — 676행 전수 조사로 채움률 0인 것만 걷어냈다.
  한국어지원 / 소유상세 / 중복소유 / 후속작대기 / 추정시기 / 목표할인율 / 국내 출시·개봉일.
  38 → 31개. `목표할인율`은 `sync_prices.py`가 이미 상수 30%로 돌고 있어 동작 변화 없음,
  `중복소유`는 `sync_steam.py`에서 계산부도 같이 뺐다(소유처 2개 이상인 행이 0건이었다).
  남겨둔 판단 보류분: `레퍼런스`(스크립트가 앞으로 채울 자리), `개인평점`·`한줄평`·`졸업일`
- **`단계` → `진행도(게임)`/`진행도(영상)`으로 재정의** (2026-08-18). 게임 어휘로 쏠려 있어 영상물에
  안 붙었고, 무엇보다 **축이 두 개 섞여 있었다** — `기대작`은 작품 쪽 상태(나왔나)이고
  나머지는 내 쪽 상태(내가 했나)다. 작품 쪽은 `이용 가능일`·`날짜정밀도`·`방영상태`가
  이미 갖고 있으므로 걷어내고, 이 속성은 **나의 진행**만 담는다.
  게임: 미확인(기본) / 출시 대기 / 구매 대기 / 보유함 / 진행 중 / 일시 중단 / 폐기됨 / 완료됨
  영상: 미확인 / 공개 대기 / 시청 가능 / 시청 중 / 일시 중단 / 폐기됨 / 시청 완료
  `일시 중단`은 다시 할 여지가 있는 것, `폐기됨`은 완전히 접은 것으로 갈랐다.

  **속성을 매체별로 나눈 이유** — 한 속성에 넣고 어휘를 중립화해봤으나
  (`보유함`인 드라마?) 계속 어색했고, 괄호 병기(`출시(공개) 대기`)는 지저분했다.
  뷰가 매체별로 갈려 있어 한 화면에 둘이 같이 보이지 않는다는 게 결정적이었다.
  중복은 값 이름뿐이고 중립인 셋은 양쪽 같은 이름을 쓴다.
  `track.py`의 `stage_of()`가 둘 중 찬 쪽을 골라주므로 나머지 로직은 하나다.
  `추천됨`은 삭제 — DB에 있다는 것 자체가 하고 싶다는 뜻이라 아무것도 안 갈랐다.
  **노션 API는 select 옵션 이름 변경을 지원하지 않는다** — 추가/삭제만 된다.
  옵션 목록을 부분만 담아 PATCH하면 나머지가 통째로 사라지고, 그 옵션을 쓰던
  행의 값이 API에서 null로 보인다(값이 지워진 건 아니라 옵션을 되살리면 돌아온다).
  이름을 바꾸려면 새 옵션을 만들고 행을 다시 찍어야 한다 → `restore_stage.py`
- 속성명: `대표출시일` → `출시·개봉일`
- **국내/해외 날짜를 나누지 않는다** (2026-08-18) — `국내 출시·개봉일`을 지웠다.
  676행 중 1건만 채워져 있었고 그 1건마저 `출시·개봉일`과 같은 날짜였다.
  아는 날짜를 넣어두고 국내 날짜가 나오면 덮어쓰면 된다. 덮어쓰면 `변경이력`에
  남고 `[날짜변경]` 알림이 그대로 간다 — 알림 경로가 하나로 합쳐지는 게 덤이다

---

## 5-1. 시리즈 완결 추적 (2026-08-18 추가)

**해결한 문제:** "완결나면 몰아보려고 대기 중인 작품"을 나무위키 방영표 열어서
매주 확인하던 것. 사용자가 예전 캘린더에 `○○ 마지막화`를 손으로 적던 이유다.

`sync_series.py`가 매일 돌면서 종류=시리즈 35행을 TMDB로 조회해
`방영상태` / `방영진행` / `완결일`을 채운다.
사람이 하는 건 **날짜정밀도를 `완결대기`로 두는 것** 하나뿐이다.

표시 자리를 두 번 옮겼다. 체크박스 → 단계 값 → **날짜정밀도 값**.
사용자 판단이고 이유가 분명하다. 완결대기는 출시 대기의 한 갈래라 진행도를
따로 빼면 추천 뷰에서 사라진다. 반면 "아직 볼 수 있는 날짜가 아니다"는
날짜정밀도가 원래 하던 말이고, `확정`이 아니니 캘린더에서도 알아서 빠진다.

- 날짜정밀도 `완결대기`인 작품이 완결되면 → **즉시 알림** ("이제 정주행 가능")
- 방영 중이면 → 완결(예정)일이 `완결일`에 들어가고, 일정 DB `최종화` 행으로도
  생겨 캘린더(.ics)에 뜬다. 추정치면 이름에 `(추정)`, 날짜정밀도 `월`
- 작품 자체는 캘린더에 안 뜬다(확정이 아니므로). 방영 시작일이 아니라
  **볼 수 있게 되는 날**만 캘린더에 있으면 된다
- 표시를 지우면 그 최종화 일정은 다음 실행에서 자동으로 정리된다
- 나머지 시리즈의 완결·취소는 일일 요약에만

**핵심 함정 — 시즌.** 노션 행은 시즌 단위(`왓이프 시즌2`)인데 외부ID는 시리즈
단위다. 최신 시즌만 보면 시즌1 행이 시즌3 방영 때문에 "방영중"으로 잘못 뜬다.
`target_season()`이 제목의 시즌 표기 → 출시일과 맞는 시즌 → 최신 시즌 순으로
대상을 정하고, `/tv/{id}/season/{n}`의 화별 방영일로 판정한다.

완결예정일은 TMDB에 마지막 화 날짜가 있으면 그 값이고, 없으면 **주간 공개 가정
추정치**다 (남은 화수 × 7일). 추정치는 특별편·결방으로 밀리므로 캘린더에
`(추정)`을 달고 날짜정밀도를 `월`로 둬서 확정 알림이 뜨지 않게 한다.

TMDB에 미래 회차 날짜가 없으면 완결 예정일이 '남은 화수 × 7일' 추정이 된다.
애니메이션이면 그 자리를 **AniList**(`adapters/anilist.py`, 키 불필요)의
회차별 방영표로 메워 확정값으로 바꾼다.

나무위키의 '회차 목록'이 같은 정보이지만 **긁을 수 없다.** 확인해봤다 —
`/raw`, `/w` 둘 다 자바스크립트로 그리는 껍데기만 오고 표가 HTML에 없다.
약관상으로도 아니다. AniList가 같은 표를 그대로 준다 (황천의 츠가이 24화 전부 일치).

만화(프리징 39건)는 어느 쪽에도 없어서 이 추적에서 빠진다. 회차 일정은 수동이다.

### 이용 가능일 — 정렬 기준 하나로

`출시·개봉일`(나오기 시작한 날)과 `완결일`(다 나오는 날)이 갈려 있어서
어느 쪽으로 정렬해도 한쪽이 어긋난다. 원본은 그대로 두고 표시용 날짜만
수식으로 뽑았다: **완결대기면 완결일, 아니면 출시·개봉일.**

`추천` 뷰(아직 안 본 단계 + 이용 가능일 내림차순)가 이걸 기준으로 돈다.
이용 가능일이 오늘이 되는 날 `track.py`가 `[볼차례]`로 한 번 알린다 —
출시 대기 작품이 조용히 과거로 밀려 흐려지는 걸 막는 자리다 (1순위 목적과 같은 일).

검증: 35행 전부 판정 성공, 재실행 시 갱신 0건(멱등).
`황천의 츠가이`(2026-08-18 추가, 넷플릭스 대기작)로 전 과정 확인 —
19/24화 · 다음 2026-08-22 · 완결예정 2026-09-19 → 최종화 일정 → .ics 반영
(작품 행은 빠지고 최종화만 들어간 것까지), 완결 전환 시 즉시 알림 문구까지.

---

## 6. 자동 매칭이 안 되는 부분 — 대조표로 고정

한글 제목이 이 프로젝트의 최대 난관이었다. 표로 만들어 커밋해뒀다.

| 파일 | 내용 | 왜 |
|---|---|---|
| `steam_map.json` | 스팀 appid ↔ 작품 제목 | 스팀은 영문, 노션은 한글. API로 이을 방법 없음 |
| `game_title_en.json` | 한글 게임 제목 → 영문 218건 | IGDB 한글 검색 히트율 10% |

**새 게임을 사거나 추가하면 이 표에 한 줄 넣으면 된다.**

TMDB는 한글 검색이 되지만 띄어쓰기를 요구한다 (`맨오브스틸` 0건, `맨 오브 스틸` 1건).
붙여쓴 제목을 2~3글자로 끊어 재조회하는 방식으로 해결했다
(`adapters/tmdb.py`의 `search_smart`).

스팀 한글 검색은 아예 응답이 없고, IGDB의 한글 별칭은 '사펑', '젤다 야숨' 같은
은어뿐이어서 못 쓴다. 이미 시도해봤으니 다시 파지 말 것.

---

## 7. 사용자가 앞으로 하는 일

새 기대작은 **노션 작품 DB에만** 넣는다 (네이버 캘린더에 직접 넣지 않는다).
최소 입력: 제목, 종류, 날짜정밀도. 날짜를 알면 출시·개봉일.

날짜를 모를 때가 이 시스템의 핵심이다.
`날짜정밀도 = 미정`, `추정시기 = "2027 상반기"`로 넣으면 캘린더에는 안 뜨지만
대기 목록에 남고, 확정되는 순간 캘린더에 등장하며 알림이 온다.

원제·외부ID·플랫폼·평점·가격은 비워둔다 — 스크립트가 채운다.

---

## 8. 남은 일

### ~~미확인 646건 정리~~ — 사용자가 2026-08-18 직접 완료. 잔여 8건

**끝났다. 646건 → 8건.** 사용자가 손으로 정리하면서 값도 자기 말로 새로 만들었다
(`구매 보류`=안 사기로, `시청 안함`=아직 안 본 것, `졸업`, `폐기됨(노잼)`).
`config.py`는 그 결과에 맞춰져 있다 — **노션에서 값을 또 바꾸면 config.py도 같이 고칠 것.**
안 그러고 `sync_schema.py`를 돌리면 옵션이 통째로 갈리면서 그 값을 쓰던 행이 비워진다.

아래는 당시 기록이다. **이 프로젝트의 1순위 목적이 여기 있었다.** 사용자의 말 그대로 옮기면,
"안 보고 안 하고 넘어간 걸 체크해서 '아 맞다 이런 게 있었지, 이거 해봐야지'"
하는 것이다. 실시간 추적 자동화는 2순위다.

그래서 이 작업은 DB를 채우는 일이 아니다. **사용자가 찍는 "안 봤음" 표시
그 자체가 결과물**이고, 그게 모여 백로그가 된다. 이 점이 방식을 결정한다.

- 기본값을 `완료됨`으로 두고 **안 본 것만 표시**하게 한다.
  13년간 직접 캘린더에 적어둔 목록이라 대부분은 실제로 봤을 것이고,
  무엇보다 표시하는 행위가 곧 백로그를 만드는 일이 된다.
  반대로 하면 본 것을 514번 찍고 나서 백로그를 또 뽑아야 한다.
- 정확도는 덜 중요하다. 완벽한 기록이 아니라 "해봐야지" 후보를 건지는 것이다.
  애매하면 넘어가도 손해가 없다.

현재 분포 (2026-08-18 실측):

| 무리 | 건수 | 처리 |
|---|---|---|
| 스팀 플레이시간 있음 | 131 | 자동 판정 가능. 최소 1시간대이고 1시간 미만은 0건이라 전부 '실제로 함'이 확실. 다만 `완료됨`인지 `폐기됨`인지는 시간만으로 못 가른다 |
| 소유처만 있음 | 1 | `보유` |
| **아무 근거 없음** | **514** | 영화 237 + 시리즈 33 포함. 나머지는 비스팀 게임이거나 플레이시간 0 |

514건 중 일부는 외부 시청기록으로 자동화할 여지가 있다.
사용자에게 어떤 기록이 있는지 확인할 것 — 왓챠피디아 별점(영화 237건에 가장 강력),
넷플릭스 시청 기록, PSN 트로피(비스팀 게임). **아직 답을 못 들었다.**
자동화되는 만큼 손으로 볼 양이 줄어든다.


### 검증 대기 — 만들었지만 아직 실전에서 안 돌아본 것

2026-08-18 새벽 작업분. GitHub 전면 장애(critical) 중이라 확인이 막혀 있다.
~~복구되면 이 다섯 개를 순서대로 확인할 것.~~
**1~3번은 2026-08-22에 확인 완료. 남은 것은 4·5번뿐이다.**

| # | 확인할 것 | 어떻게 |
|---|---|---|
| 1 | ~~`calendar.yml`이 5분마다 도는가~~ | **확인됨(2026-08-22)** — 돌긴 도는데 5분이 아니라 16~30분 간격 |
| 2 | ~~예약(cron) 실행이 되는가~~ | **확인됨(2026-08-22)** — 두 워크플로 모두 `schedule`로 성공 중 |
| 3 | ~~예약이 얼마나 밀리는가~~ | **확인됨(2026-08-22)** — calendar 16~30분, daily 40분. 3절 참고 |
| 4 | 클라우드 루틴 첫 실행 | 매일 10시. 루틴 id `trig_01KA1sf3swhxodBx7Rp7A1Ne` |
| 5 | 두 워크플로 동시 push 충돌 | `pull --rebase` 재시도를 넣었으나 실제 충돌은 안 겪어봤다 |

`heartbeat.py`는 매월 1일에만 보내므로 다음 실전은 9월 1일이다.
지금 확인하려면 `python heartbeat.py --force`.

### 사용자 작업

| # | 작업 | 메모 |
|---|---|---|
| 1 | ~~`ITAD_API_KEY` 시크릿 등록~~ | **완료 (2026-08-17 등록 확인).** 시크릿 6종 전부 등록됨. 가격 추적이 실제로 도는지는 다음 실행 로그로 확인할 것 |
| 2 | **완결 기다리는 시리즈의 날짜정밀도를 `완결대기`로** | 현재 `황천의 츠가이` 1건. 이 표시가 있어야 완결 알림·캘린더가 붙는다 |
| 3 | 기대작 보드 카드에 `방영진행`·`완결일` 노출 | 뷰 설정은 API로 못 건드려 수동. 이러면 별도 뷰 없이 기대작 뷰에서 진행 상황이 보인다 |

https://github.com/jo6417/release-tracker/settings/secrets/actions

### 개발 계획 (2026-08-22 전면 재검토)

**번호가 곧 개발 순서다.** 세트 단위로 묶었고, 앞 세트가 뒤 세트의 기반이다.

| 세트 | # | 작업 | 비용 |
|---|---|---|---|
| **A** 준비 | 1 | 노션 수동 2건 — `완결대기` 표시, 기대작 보드에 `방영진행`·`완결일` 노출 | — |
| | 2 | 문서 갱신 — 성공기준 3번 문구, 이 표 | — |
| | 3 | 병합 완료된 브랜치 2개 정리 | — |
| **B** 임박 펄스 | 4 | `refresh_dates.py`를 `확정` 건까지 확장 — 연기 감시. **노션은 안 덮고 알림만** | S |
| | 5 | `pulse_state.json` 신설 + daily.yml 커밋 스텝 | S |
| | 6 | D-7 / D-1 펄스 파트 + 브리핑 `7일 내 공개` 칸 삭제 | S |
| **C** 후보 큐 | 7 | `candidates.json` + 고정번호(`c17`) + 저녁 카드 후보 파트 | M |
| | 8 | `apply_candidates.py` + CLAUDE.md 채팅 승인 규칙 | S |
| | 9 | 스팀 위시리스트 수집 → 큐 투입 | S |
| **D** 매칭 위생 | 10 | `match_igdb`·`match_tmdb` 주간 워크플로 | S |
| | 11 | 외부ID 없는 29건 채우기 | M(손) |
| **E** 구독 감지 | 12 | 게임패스 카탈로그 ID diff + healthcheck 경보 | M |
| | 13 | Xbox Wire 태그 피드 파서 (이탈 예고) | S |
| | 14 | `gamepass_map.json` 매칭 + DB 유무 분기 | M |
| | 15 | TMDB `watch/providers` — 넷플릭스·프라임·쿠팡플레이 | S |
| **F** 확장 | 16 | IGDB 프랜차이즈 후속작 → 큐 | S~M |
| | 17 | "아직 보는 중?" 확인 질문 | S |
| **G** | 18 | 취향 큐레이션 (`similar_games`/`recommendations`) → 큐 | M |

기반이 셋이고 나머지가 거기 얹힌다. **후보 큐**(세트C)에 9·14·16·18이 물리고,
**스냅샷 diff**(세트E)를 게임패스와 OTT가 공유하며, **"한 번만 묻고 기록"하는
상태 파일**(세트B)을 임박 펄스와 17번이 공유한다.

4와 6을 쪼개면 안 된다. 연기 감지 없이 펄스만 붙이면 연기된 작품에 "내일
출시"라는 틀린 알림이 나간다.

### 결정된 설계 (2026-08-22, 사용자 확인)

- **자동 유입은 작품 DB에 바로 넣지 않는다.** 후보 큐에 쌓고 저녁 카드로 제시한 뒤,
  사용자가 **클로드 채팅으로 승인/거부**하면 그때 등록한다. 무응답은 보류다.
  노션 체크박스도 후보 DB도 쓰지 않으므로 저장소는 `candidates.json` 하나면 된다
- 제시는 **저녁 카드에만 최대 2건**, 미응답 **대기 상한 5건**. 상한에 닿으면 신규 제시를
  멈추되 발굴은 계속한다(놓치지 않는 것이 1순위 목적이다). 같은 후보만 반복되지 않게
  `pick_backlog`의 날짜 커서를 재사용해 돌려가며 제시한다
- 알림은 **하루 두 장을 유지한다.** 새 기능은 전부 스풀에 파트로 들어가고 카드를 늘리지 않는다
- 임박 펄스는 **`출시 대기`만** 대상. **D-7 / D-1 / D-0** 세 번이며 D-0은 브리핑의
  `오늘 공개` 칸이 이미 하고 있다. 날짜정밀도가 `월`·`분기`면 제외한다(D-day 계산이 안 된다).
  연기가 잡히면 보낸 마크를 리셋한다
- 연기 감지 시 **노션을 덮지 않고 알림만 보낸다.** 08-21에 자동 덮어쓰기로 손으로 찍은
  6건이 날아간 사고가 있었다. 1~2주 지켜보고 소스가 믿을 만하면 자동 반영으로 바꾼다
- 시청 진행도는 **새 속성을 만들지 않는다.** `진행 중`인데 21일 넘게 갱신이 없으면
  저녁에 한 건만 "아직 보는 중?"을 묻고, 채팅 답을 받아 `마지막플레이일`을 갱신한다
- 구독 서비스는 **게임패스(엑박, 구독 중) + 넷플릭스·아마존 프라임·쿠팡플레이**

### 게임패스 — 경로를 실측으로 확인했다 (2026-08-22)

"공식 API가 없다"를 "방법이 없다"로 잘못 적어뒀었다. 실제로는 세 경로가 살아 있고
전부 200을 준다.

- `catalog.gamepass.com/sigls/v2?id=<GUID>&language=ko-kr&market=KR`
  — 콘솔 `f6f1f99f-9b49-4ccd-b3bf-4d9767a77f5e` 558건 / PC `fdd9e2a7-0fee-49f6-ad69-4354098401ff` 472건
- `displaycatalog.mp.microsoft.com/v7.0/products?bigIds=...&market=KR&languages=ko-kr`
  — 한국어 제목과 개발사를 준다. 요청당 20개 남짓씩 끊어 보내야 한다
- `news.xbox.com/en-us/tag/xbox-game-pass/feed/`
  — **전체 피드(`/feed/`)는 11건만 담겨 게임패스 글이 하루 이틀이면 밀려난다. 태그 피드를 쓸 것.**
  `Coming to XBOX Game Pass: ...` 글에 입점 예정일과 `Leaving <날짜>` 목록이 들어 있다

문서화된 API가 아니므로 예고 없이 바뀔 수 있다. 그래서 **healthcheck에 "카탈로그가
0건이거나 절반으로 급감" 경보를 기능과 같은 커밋에 넣는다.** 나중에 붙이면 조용히
죽는 구간이 생긴다.

매칭이 유일한 실비용이다. 제목이 한국어에 `®`·`일반판`·`컴플리트 에디션`이 붙어 그대로는
안 맞는다. 그러나 **매일은 ProductId 목록만 비교하면 되고**(입점·이탈 diff는 ID 기준이라
정확하다), 신규 ID가 뜬 날에만 이름 매칭을 1회 시도해 `gamepass_map.json`에 쌓는다.
`steam_map.json`과 같은 구조다.

OTT와는 성격이 반대다. **TMDB는 문서화된 API지만 이탈 예정일을 주지 않아 사후
통보가 되고, 게임패스는 비공식이지만 2주쯤 전에 예고가 온다.** "내려가기 전에
해치우자"가 되는 쪽은 게임패스뿐이다.

### 왜 발굴이 급한가 — DB에 없으면 아무것도 못 한다

트레일러 감지를 붙이면서 드러난 것이다. 이 시스템은 **작품 DB에 있는 것만**
본다. 블랙 미스: 종규는 2025-08-19에 발표됐는데 DB에 없어서, 1년 동안 티저도
설 특집 영상도 15분 게임플레이도 전부 지나갔다. 코드가 아니라 목록의 문제였다.

지금 막아둔 구멍은 하나다 — `youtube_watch.json`의 채널 감시는 DB에 없는
작품도 잡는다. 하지만 채널을 사람이 등록해야 한다.

자동으로 메우는 경로가 세트C·E·F·G다. `검은신화 오공`(igdb 136879)의 프랜차이즈를
따라가면 `Black Myth: Zhong Kui`(igdb 361900)가 나온다. **원안은 이런 신작을 진행도
`출시 대기`로 바로 넣는 것이었으나 폐기했다** — 08-21에 세운 "진행도는 사람이 정한다"와
정면으로 부딪힌다. 발견한 것은 후보 큐로 보내고, 등록 여부는 사용자가 채팅으로 답한다.

매칭 스크립트(`match_igdb.py`, `match_tmdb.py`)는 매일 실행에 들어 있지 않다.
채팅으로 노션에 직접 추가한 작품은 외부ID가 빈 채로 남는다. `refresh_dates.py`가
외부ID 있는 행만 재조회하므로 이 공백은 **가격뿐 아니라 날짜 추적에서도 빠진다**는
뜻이다. 세트D에서 주간 실행으로 막는다.

---

## 9. 성공 기준 현황

| # | 기준 | 상태 |
|---|---|---|
| 1 | 미정 작품 날짜 확정 시 하루 안에 알림 | **달성** |
| 2 | 캘린더 수동 수정 0회 | **달성** — 폰 캘린더 구독까지 확인 (2026-08-18) |
| 3 | **등록해둔 작품이 내 구독 서비스에 들어오면 그날 알림이 온다** | 미착수 (세트E) — 원문은 "게임패스 입점을 찾아보지 않아도 알게 됨"이었다. 주어가 빠져 뜻이 흐렸고, 무엇보다 공식 API가 없는 게임패스 하나에 기준이 묶여 영영 미달로 남았다. 사용자가 실제로 구독하는 것(게임패스·넷플릭스·프라임·쿠팡플레이) 전부로 범위를 넓혔다 |
| 4 | 할인 알림에 소유 여부 표시 → 중복구매 0회 | **달성.** 2026-08-21 판정 기준을 소유처 → 진행도 화이트리스트로 교체(오탐 112건 후보 제거) |
| 5 | 6개월 뒤에도 계속 쓰고 있음 | 시간이 답할 문제. 60일 자동 종료 감시를 두 겹 붙여 대비 |

---

## 10. 작업 시 주의사항

- **실행 전 확인**: 대량 Notion 입력, 파일 삭제, 리포 공개 전환 등
  되돌리기 어려운 작업은 먼저 확인
- **토큰 취급**: 로그·커밋·채팅에 절대 출력 금지
- **한글 우선**: 변수명은 영문이어도 되나 Notion 속성명·알림 문구·작품명은 한글
- **과설계 금지**: 개인용 도구. 사용자 1명. 인증·정산·확장성 불필요
- **60일 자동 종료**: GitHub은 public 리포에 60일간 활동이 없으면 예약
  워크플로를 자동으로 끈다. 워크플로 자신이 만든 커밋은 활동으로 쳐주지
  않는 것으로 알려져 있어, 자동 커밋으로 타이머를 되돌리는 방법은 통하지 않는다.
  사용자는 메일을 거의 보지 않으므로 GitHub의 경고 메일에 기대면 안 된다.
  그래서 감시를 두 겹으로 뒀다.
  - `heartbeat.py` — 매월 1일 노션 멘션으로 생존 신호. 안 오는 달이 있으면 멈춘 것
  - `healthcheck.py` — 워크플로 실행 이력을 밖에서 확인. 매일 10시 예약 작업으로
    자동 실행되며, **이상이 있을 때만** 사용자에게 보고한다
    (예약 작업 id: `release-tracker-healthcheck`)

  **커밋 유무로 판단하지 말 것.** DTSTAMP를 노션 수정시각 기준으로 바꾼 뒤로는
  신작이 없으면 커밋이 안 생기는 게 정상이다. 정확한 신호는 워크플로 실행 이력이고,
  `calendar.yml`은 데이터가 그대로여도 계속 돌기 때문에(실측 16~30분 간격)
  그게 한참 끊기면 확실한 고장이다.
- **유지보수가 최대 리스크**: 복잡할수록 방치될 확률이 올라간다.
  추가 토큰·추가 계정·수동 갱신이 필요한 방안은 되도록 피한다
- **셸 heredoc에서 백슬래시가 뭉개진다**: 정규식의 `\b` `\n` `\s`나
  윈도우 경로가 든 내용을 heredoc으로 파일에 쓰면 깨진다.
  `chr(92)`로 조립하거나 줄 단위로 패치하거나 raw 문자열을 쓸 것.
  이 프로젝트에서 여러 번 걸렸다
- **오매칭을 의심할 것**: 사용자가 "이거 날짜 이상한데"라고 하면 대체로 맞다.
  고스트리콘이 2001년 원작에 붙은 사례가 있었다 (실제로는 2017 와일드랜드 베타).
  부속 일정보다 출시일이 크게 앞서면 다른 작품에 붙은 신호다
- **알림 설계**: 처음 만들 때 목표가 알림이 107건 한꺼번에 울렸고, 대부분
  "이미 보유중"이었다. 알림은 (1) 살 마음이 있다고 표시된 것만 (2) 이전 값과
  비교해 새로 조건에 들어온 것만 보낸다. 첫 조회는 기준값만 잡는다
- **비어 있는 칸을 "아니다"로 읽지 말 것**: (1)의 판정을 처음에는 `소유처`가
  비었는지로 했는데, 소유처는 채우다 만 칸이라 졸업 197건 중 112건이 비어
  있었다. 그게 전부 "미보유"로 통과해서, 2026-08-21에 이미 졸업한 엔세스터가
  "→ 미보유 · PS4"로 추천됐다. 세일 시즌이었으면 알림 폭탄이었다.
  **보유 여부는 소유처가 아니라 진행도가 안다.** 지금은 진행도 화이트리스트
  (`구매 대기`·`출시 대기`)로 판정한다 — 후보가 402 → 62건
