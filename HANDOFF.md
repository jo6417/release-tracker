# 출시 트래커 — 인계 문서

> Claude Code에게 전달하는 문서. 이 파일 하나로 맥락 파악이 되도록 작성됨.
> 상세 정의는 `PROJECT.md`, 스키마는 `SCHEMA.md` 참조.

---

## 0. 먼저 읽을 것

1. `PROJECT.md` — 목적, 문제 정의, 범위, 리스크
2. `SCHEMA.md` — Notion DB 속성 전체 정의
3. 이 문서 — 현재 상태와 다음 작업

---

## 1. 한 줄 요약

게임·영화·드라마·애니·만화의 **출시일을 자동 추적**하는 개인용 시스템.
GitHub Actions가 매일 1회 API를 조회해 변경분만 Notion에 기록하고, Notion이 UI·캘린더·알림을 전부 담당한다.

**서버 없음. 프론트엔드 없음. 앱 없음.**

---

## 2. 사용자 정보

- 이름: 조덕근 (부산, 게임 개발자, Unity)
- Notion을 이미 일상적으로 사용 중 (식단·운동·체중·구매목록 DB 보유)
- 감독 역할만 수행. **코딩은 전부 Claude가 담당**한다는 전제
- 한글 UI 선호. 작품명도 한글 우선 (13년간 캘린더에 한글로 기록해옴)

---

## 3. 아키텍처

```
매일 09:00 KST   GitHub Actions (약 30초)
     |
     |  1. 조회  — IGDB / TMDB / KOBIS / ITAD / Steam / AniList / JustWatch
     |  2. 비교  — 어제 스냅샷과 대조 (날짜·정밀도·가격·게임패스·평점)
     |  3. 기록  — 변경분만 Notion에 write
     v
  Notion 2개 DB  <-->  사용자가 직접 수정
     |
     +--> 캘린더 뷰 (날짜 확정된 것만)
     +--> 보드 뷰 (상태 관리)
     +--> 멘션 푸시 알림 (하루 0~2건)
```

### 설계 원칙

- **의존성 최소화**: Python 표준 라이브러리 + requests 정도. 프레임워크 없음
- **이식성**: 순수 Python이라 GitHub Actions가 막히면 로컬 크론으로 즉시 이전 가능
- **소스 격리**: 데이터 소스마다 어댑터 파일 1개. 하나가 깨져도 나머지는 정상 동작
- **원본 보존**: API 응답 스냅샷을 리포에 커밋 → 파서가 깨져도 데이터는 남음
- **조용한 실패 방지**: 3일 연속 같은 소스 실패 시 알림

### 핵심 개념 2가지

**(1) 날짜 정밀도** — 출시일을 단일 날짜가 아닌 등급으로 저장
```
미정 → 연도 → 분기 → 월 → 확정
```
확정된 것만 캘린더에 노출. 정밀도가 올라가면 자동 승격 + 알림.
**이것이 이 프로젝트가 존재하는 이유다.** (미정이라 캘린더에 못 넣어서 잊어버리는 문제)

**(2) 3단계 생애주기**
```
기대작(놓치지 않기) → 구매판단(돈 아끼기) → 플레이(내가 기록하기)
                                              └→ 후속작 대기 → 1단계로 순환
```

---

## 4. 현재 상태

*(2026-08-17 갱신)*

### 완료

- [x] 문제 정의, 범위 확정, 기술 선택
- [x] 네이버 캘린더 .ics 분석 (1,058건, 2013~2027)
- [x] **X-NAVER-STICKER 발견** — 스티커 ID로 게임/영화 구분이 보존됨
- [x] Notion 통합 + GitHub 리포 + API 키 5종 발급·검증
- [x] **노션 DB 2개 생성** (작품 34속성 / 일정 10속성, 양방향 관계)
- [x] **구조 재설계** — 아래 '구조 변경' 참조
- [x] **이관 데이터 정리** — 1,058건 → 작품 585 / 부속 일정 29
- [x] **TMDB 어댑터 + 매칭** — 영화·시리즈 273건 전부 매칭
- [x] 585작품 실제 이관

### 구조 변경 (2026-08-17)

원안은 모든 날짜를 일정 DB 행으로 만들었으나, 97%가 일정 1개짜리라
같은 날짜를 작품과 일정 두 곳에 쓰는 중복이 생겼다. 그래서:

- **메인 날짜(정식출시·극장개봉·OTT공개·시즌시작)는 작품 행이 직접 보유**
- 부속 사건(베타·데모·DLC·예약구매)만 일정 행 → 669행에서 29행으로
- 순차 공개 드라마는 `출시·개봉일`을 기간으로 (쉬헐크 2022-08-17 ~ 10-13)
- 종류를 `게임/영화/시리즈/만화` 4종 + `애니메이션` 체크박스로 (조합 폭발 방지)
- 과거 기록은 `완료`가 아니라 **`미확인`** 단계로 (실제로 했는지 모르므로)

### 이관 시 걸러낸 것들

| 문제 | 처리 |
|---|---|
| 스티커 574(노란별)가 2015년 이후 잡다한 표시로 용도 변경 | 2015-06-01 이전만 게임으로 인정 |
| 스티커 501(기본)에 개인 일정 사이 출시가 섞임 | 61건 전수 확인 후 8건만 선별 |
| 'NN 프리징'을 만화 회차로 오분류 | 회사 빌드 프리징이라 제외 |
| 제목에 섞인 플랫폼·OTT·게임패스 표기 | 속성으로 분리 (플랫폼 32, 공개처 5) |
| 오타 (Sword o the sea, 사힐런트힐f, 뎅케르크 등) | 교정표로 수정 |
| 같은 작품 중복 기록 (인투더스톰 08-08/08-28 등) | 병합 또는 삭제 |
| TMDB 한글 검색이 띄어쓰기를 요구 | 붙여쓴 제목을 2~3글자로 끊어 재조회 |

### 미완료

- [ ] IGDB 매칭 (게임 310건 외부ID)
- [ ] 스팀 라이브러리 동기화 (키 검증됨, 227게임 조회 확인)
- [ ] 추적 스크립트, 스냅샷, 알림, GitHub Actions
- [ ] 노션 뷰 구성

## 5. 자격 증명 상태

| 키 | 상태 | 비고 |
|---|---|---|
| `NOTION_TOKEN` | ✅ 로컬 .env + 시크릿 | |
| `TMDB_API_KEY` | ✅ 로컬 .env + 시크릿 | 검증 완료 |
| `TWITCH_CLIENT_ID` | ✅ 로컬 .env + 시크릿 | 검증 완료 |
| `TWITCH_CLIENT_SECRET` | ✅ 로컬 .env + 시크릿 | 토큰 발급 확인 |
| `STEAM_API_KEY` | ✅ 로컬 .env | SteamID 76561197996869791 |

**GitHub 시크릿은 쓰기 전용이라 값을 다시 볼 수 없다.** 로컬 `.env`가 사본 역할을 한다.

**주의**: 토큰은 채팅에 노출하지 말 것. `.env`(gitignore됨) 또는 GitHub 시크릿에만.
과거에 한 번 노출되어 폐기·재발급한 이력 있음.

### Notion 상수

```
부모 페이지 ID: 3be857fe-7a80-808e-b6ad-c595ff14a3e8   (🎯 출시 트래커)
GitHub 리포:   jo6417/release-tracker  (private)
```

---

## 6. 즉시 할 일

### Step 1 — DB 생성

```bash
export NOTION_TOKEN=ntn_...        # 또는 .env 사용
python create_db.py
```

**사전 조건**: 부모 페이지에서 `⋯` → 연결 → "출시 트래커" 통합 선택.
안 되어 있으면 권한 오류 발생. 이건 사용자가 Notion UI에서 직접 해야 함.

성공 시 `db_ids.json` 생성됨.

### Step 2 — 이관 검증

```bash
python migrate.py --dry
```

`migrate.py` 상단의 `ICS` 경로를 로컬 .ics 파일 위치로 수정할 것.

### Step 3 — 실제 이관

```bash
python migrate.py
```

613작품 + 670일정. Notion 3req/s 제한 때문에 **약 10분 소요**.
중단되면 중복이 생기므로, 재실행 전 DB를 비울 것.

### Step 4 — 커밋

`.env`, `db_ids.json`이 gitignore에 있는지 확인 후 push.

---

## 7. 이후 로드맵

### 1차 마일스톤 — 날짜 추적 자동화 (핵심)

| # | 작업 |
|---|---|
| 1 | 어댑터 구조 설계 (`adapters/igdb.py`, `tmdb.py` ...) |
| 2 | 이관된 작품에 외부ID 매칭 (제목 → IGDB/TMDB 조회) |
| 3 | 추적 스크립트: 날짜·정밀도 변화 감지 |
| 4 | 스냅샷 저장 (`snapshots/YYYY-MM-DD.json` 커밋) |
| 5 | Notion 멘션 알림 (일일 요약 + 즉시 알림 2종) |
| 6 | GitHub Actions 크론 (`.github/workflows/daily.yml`) |

여기까지가 **원래 불편의 80% 해소** 지점.

### 2차 — 돈 관련

| # | 작업 |
|---|---|
| 7 | Steam 라이브러리 동기화 (소유 게임 + 플레이시간) |
| 8 | 게임패스 입점 감지 |
| 9 | ITAD 가격 추적 + 목표가 알림 (**소유 여부 함께 표시**) |
| 10 | .ics 생성 → GitHub Pages → 캘린더 구독 |

### 3차 — 있으면 좋은 것

| # | 작업 |
|---|---|
| 11 | 방치 감지 (마지막플레이일 60일 초과) |
| 12 | 후속작 자동 등록 (IGDB 프랜차이즈 관계) |
| 13 | 취향 추천 (13년치 이력 613건이 시드) |

**각 단계가 끝날 때마다 그 자체로 쓸모가 있어야 한다.**
중간에 흥미가 식어도 손해가 없도록.

---

## 8. 데이터 소스

### 안정 (공식 API)

| 소스 | 용도 | 키 |
|---|---|---|
| IGDB (Twitch) | 게임 메타데이터, 출시일, 프랜차이즈 | 필요 |
| TMDB | 영화·드라마, 지역별 개봉일, 시즌·에피소드 | 필요 |
| KOBIS (영진위) | 국내 극장 개봉일 공식 데이터 | 무료 발급 |
| IsThereAnyDeal | 가격 이력, 역대 최저가 (**SteamDB 대체**) | 필요 |
| AniList | 애니 분기 신작 | 불필요 |
| JustWatch | 국가별 스트리밍 제공처 (국내 OTT) | 불필요 |
| Steam Web API | 소유 라이브러리 + 플레이시간 | 필요 |

**SteamDB는 스크래핑 금지. 반드시 ITAD를 쓸 것.**

### 준안정

- Steam appdetails: 비공식이나 10년+ 유지. 가격·에디션·평가

### 취약 (어댑터 격리 필수)

- 게임패스 카탈로그: 공식 API 없음. **가장 깨지기 쉬운 지점**
- PS스토어 / 닌텐도 e숍 한국 가격: 반영 불완전

### 플랫폼별 소유 자동 수집

| 플랫폼 | 자동 | 대안 |
|---|:---:|---|
| 스팀 | O | 공식 API |
| 게임패스 | △ | 카탈로그 대조 |
| PS / Xbox | △ | 토큰 필요, 불안정 |
| 에픽 | X | **주간 무료배포 목록으로 우회** |
| GOG / 닌텐도 | X | 수동 입력 |

---

## 9. 알림 사양

### 즉시 알림 (별도 푸시)

1. 정식출시/극장개봉/국내개봉의 날짜정밀도가 `확정`으로 변경
2. 게임패스 체크박스 false → true
3. 현재최저가 ≤ 정가 × (1 − 목표할인율)
   → **반드시 소유처를 함께 표시** (중복 구매 방지가 목적)

예시:
```
[할인] GTA VI 30% (₩49,000)
[경고] 이미 보유중 — 스팀 (2020-05 무료배포)
```

### 일일 요약 (하루 1건, 변경 있을 때만)

날짜 변경/연기, 가격 변동, 평점 갱신, 신규 추천, 베타·데모 일정 추가

변경이 없는 날은 **아무 알림도 보내지 않는다.**

---

## 10. 성공 기준

1. 미정 작품의 날짜가 확정되면 **하루 안에** 알림
2. 캘린더 수동 수정 **0회**
3. 게임패스 입점 여부를 **찾아보지 않아도** 알게 됨
4. 할인 알림에 소유 여부가 표시되어 **중복 구매 0회**
5. **6개월 뒤에도 계속 쓰고 있음** ← 이전 게임 DB가 11개월 방치로 실패한 지점

---

## 11. 주의사항

- **실행 전 확인**: 613건 Notion 입력, 파일 삭제, git push 등 되돌리기 어려운 작업은 사용자에게 먼저 확인
- **토큰 취급**: 절대 로그·커밋·채팅에 출력하지 말 것
- **한글 우선**: 변수명은 영문이어도 되나, Notion 속성명·알림 문구·작품명은 한글
- **과설계 금지**: 개인용 도구. 사용자 1명. 인증·정산·확장성 불필요
- **유지보수가 최대 리스크**: 복잡하게 만들수록 방치될 확률이 올라간다
