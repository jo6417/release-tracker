# 설계 — 게임쇼 추적

2026-09-12 확정.

## 풀려는 문제

게임쇼는 **놓친 작품이 한꺼번에 쏟아지는 자리**다. 2026-09-09 닌텐도
다이렉트에서 86건이 공개됐고 그중 6건이 작품 DB에 있는 관심작이었는데,
지금 시스템은 그 사실을 전혀 모른다. HANDOFF 1순위("놓치고 넘어간 작품
발견")와 정확히 같은 성격이다.

## 설계 전에 확인한 사실

### 일정 DB에 `종류=발표`가 이미 있고 비어 있다
옵션도 있고 `make_ics.py:244`에 🎮 아이콘까지 매핑돼 있는데 43건 중 **사용 0건**.

### IGDB `/events` 가 지금 자격증명으로 그대로 열린다 (검증 완료)

```
Nintendo Direct 2026.09.09     2026-09-09 23:00 KST   게임 86건
State of Play: 2026-9-3        2026-09-03 22:00 KST   게임 34건
Gamescom Opening Night Live    2026-08-26 03:00 KST   게임 73건
```

시각까지 정확하고, 각 이벤트가 공개한 게임이 **IGDB game id 배열**로 들어
있다. 작품 DB에 igdb ID를 가진 행이 266건이라 **제목 매칭 없이 ID로 정확히
대조된다.** 실제 대조 결과:

| 이벤트 | 공개 | 관심작 | 누구 |
|---|---|---|---|
| Nintendo Direct 9/9 | 86 | 6 | Tomb Raider: Legacy of Atlantis, 레지던트이블3, 커비의 에어라이더, 007 First Light, 헬이즈어스, 가디언즈 오브 갤럭시 |
| State of Play 9/3 | 34 | 1 | 울버린 |
| Gamescom ONL 8/26 | 73 | 8 | 애니모, 귀무자, SILENT HILL: Townfall, CONTROL: Resonant 외 4 |

### 미래 일정은 IGDB에 없다
`where start_time > now` → **0건**. IGDB는 사후 등록형이다.

### 유튜브 RSS는 사전 예고에 쓸 수 없다
`feeds/videos.xml`은 **이미 올라간 영상만** 준다(닌텐도·PS·Xbox·IGN 4개 채널
확인). 쇼가 끝난 뒤에 잡히는 경로다.

## 결정

**세 문제를 나눈다.**

| | 자동화 | 방법 |
|---|---|---|
| 쇼가 언제 몇시에 (미래) | 반자동 | 연례는 수동 1회, 불시는 유튜브 예약 스트림 |
| 무슨 작품이 나왔나 (직후) | 완전 자동 | IGDB events ∩ 작품 DB |
| 뭐가 나올 것 같나 (추측) | 자동 | 작품 DB의 플랫폼+날짜정밀도로 후보 뽑기 |

**어디에 넣나: 일정 DB, `종류=발표`.** 새 DB를 만들지 않는다.
캘린더(.ics)에 자동으로 실려 네이버 캘린더까지 그대로 간다.
`작품` relation이 multi라 한 쇼에 N개 작품을 달 수 있다.

**이름은 IGDB가 주는 원문 그대로 쓴다.** 한글 대조표를 만들지 않는다.
이름이 길면 뒤의 날짜 부분만 자른다.

## 스키마 — 일정 DB에 3개 추가

| 속성 | 타입 | 용도 |
|---|---|---|
| `기대작` | rich_text | 작품 DB에 아직 없는 미발표 신작 소문 |
| `스트림` | url | IGDB `live_stream_url` |
| `외부ID` | rich_text | `igdb_event:1234` — 중복 방지·재조회 키 |

시각은 기존 `날짜` date 속성이 담는다. **작품 DB는 변경 없음.**

## 필수 코드 수정 — `make_ics.py`

`make_ics.py:200`이 `DTSTART;VALUE=DATE`로 **종일 일정만** 만든다.
23:00 다이렉트가 캘린더에 "9월 9일 하루종일"로 뜬다.
시각이 있는 행은 `DTSTART;TZID=Asia/Seoul`로 내보내야 한다.

**게임쇼에서 시각은 정보의 거의 전부다. 이걸 빼면 기능이 성립하지 않는다.**
나머지 모든 작업의 전제이므로 가장 먼저 한다.

## `sync_events.py` (하루 1회)

```
IGDB /events 조회 (지난 10일)
  → 일정 DB에 종류=발표 행 생성 (외부ID로 중복 방지)
  → games 배열 ∩ 작품 DB의 igdb ID → 작품 relation 연결
  → 남은 것은 확인 큐로 ("등록할까요?")
  → 아침 카드: "[게임쇼] 어제 Nintendo Direct — 관심작 6건 등장"
```

한 번에 80건이 후보로 쏟아질 수 있다. **몇 건까지 올릴지는 만들 때 정한다.**

## 사전 예고

### 연례 대형 행사 — 수동 1회
TGS·gamescom·TGA·Summer Game Fest 등 연 5~6개. 날짜가 몇 달 전 확정된다.
`events_known.json`에 적어두거나 채팅 한 줄("9/25 23시 TGS 넣어줘").
자동 탐색을 만들 값어치가 없는 규모다.

### 불시 쇼케이스 — 유튜브 예약 스트림
닌텐도·PS·Xbox가 다이렉트를 예고하면 **유튜브에 "예정된 라이브 스트림"
페이지가 먼저 생긴다.** 제목과 시작 시각이 그 시점에 확정돼 있다.
YouTube Data API `search?eventType=upcoming&channelId=...` 로 잡는다.
키 필요(무료), 하루 4개 채널이면 쿼터의 1% 미만.

보조로 게임 뉴스 RSS를 `Direct`·`State of Play`·`Showcase` 키워드로 거르는
방법이 있으나 시각을 본문에서 파싱해야 해 부정확하다. 유튜브가 잡히면
그걸 쓰고 못 잡을 때만 뉴스를 본다.

**구글 검색을 매일 돌리는 건 권하지 않는다.** Actions에서 구글을 긁으면
봇 차단에 걸리고, 검색 API를 쓰더라도 결과 텍스트에서 "9월 25일 오후 11시"를
정확히 뽑는 건 LLM 없이는 자주 틀린다.

### 알림
```
[게임쇼] 7일 후 · 9/25 (목) 23:00  Tokyo Game Show 2026
[게임쇼] 내일 23:00  Nintendo Direct 2026.09.09
           지난 3회에서 관심작이 평균 5.7건 나왔습니다
```

**네이버 캘린더 주의**: 구독 .ics를 몇 시간~하루 주기로 당겨간다.
"내일 다이렉트"를 오늘 잡으면 네이버 알림은 못 갈 수 있다.
7일 전에 잡히는 연례 행사는 확실히 가고, 불시 쇼케이스는 아침 카드가
주 경로이고 네이버가 보조다.

## 추측("뭐가 나올 것 같다")

쇼 전날 카드에 근거 두 줄을 붙인다.

```
[게임쇼] 내일 23:00 Nintendo Direct
  지난 3회에서 관심작이 평균 5.7건 나왔습니다
  나올 만한 것: 커비 월드 비욘드, 젤다 무쌍 2, 메트로이드 프라임 4
  (플랫폼=Switch2 + 날짜정밀도가 미정·분기인 작품 DB 행)
```

작품 DB의 `플랫폼`과 `날짜정밀도`만으로 뽑는다. 새 데이터가 필요 없고
틀려도 손해가 없다.

## 기각 — 유튜브 채널 등록 (검증 후 취소, 2026-09-12)

처음엔 "코드 0줄짜리 덤"이라고 적었으나, 구현 직전에 확인해보니 틀렸다.

`youtube_videos()`(RSS 경로, `youtube_watch.json`이 쓰는 함수)에는
예고편·티저 필터가 없다. `VIDEO_KIND` 필터(`sync_media.py:175`)는
TMDB 경로에만 걸려 있다. 즉 **채널이 올리는 모든 영상**이 그대로
"새 영상"으로 잡힌다.

닌텐도 공식 채널 실제 업로드를 확인하니 "Just Dance 예고편",
"Stellar Blade 예약구매 트레일러" 같은 무관한 영상이 하루에도 여러 건
섞여 있었다. 그대로 등록하면 매일 "[신규 예고편] 닌텐도"가 몇 건씩
뜨는데, 원하는 신호("우리 관심작이 다이렉트에 나왔나")와 다르다.
**소음이 신호를 덮는다.**

`sync_events.py`(IGDB events ∩ 작품 DB)가 이미 정확한 대체재다 — 실제로
86건 중 관심작 6건만 정확히 골라냈다. 유튜브 채널 감시는 만들지 않는다.

## 커버리지 한계

작품 DB 421건 중 igdb ID 보유는 **266건**. steam ID만 있는 행은 게임쇼
대조에서 빠진다. `igdb.steam_appids()` 로 역매핑하면 늘릴 수 있다.
