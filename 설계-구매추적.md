# 설계 — 구매·배송 추적

2026-09-12 확정. 세션이 바뀌어도 이 문서만 읽으면 이어서 만들 수 있다.

## 풀려는 문제

예약구매·해외구매는 **결제와 수령 사이가 길다.** 그 사이에 산 사실을 잊는다.
"이거 샀나?" 싶어 크롬 기록·카드 결제내역·쇼핑몰 주문내역을 한참 뒤진다.
게임만의 문제가 아니다 — 생활용품 직구도 같다.

## 설계 전에 확인한 사실

- 작품 DB 33개 속성 중 "샀는지·어디서·언제·어디쯤 왔는지"를 담는 칸이 **없다**
  - `소유처`는 손에 넣은 **뒤**의 위치, `획득경로`는 방식, `진행도=구매 대기`는 **사야 할 것**
  - 이미 결제한 amiibo 미넬 골렘이 지금도 `구매 대기`로 남아 있다
- `정가`·`현재최저가`는 sync_prices가 조회해 넣는 시세라 **실제 결제액과 무관**하다
- 🛒 구매DB(`3ae857fe-7a80-8059-bdce-ed16e460a4f9`)는 이미 있으나 **살림 메모장**이다
  - 16건 전부 `구매일` 비어 있음, `링크` 비어 있음, `주문·배송 중` 0건
  - **손으로 채우는 칸은 안 채워진다.** 입력 비용을 0에 가깝게 만드는 것이 설계의 절반
- 출시 트래커 통합 토큰이 구매DB에 **접근 못 한다 (404)**

## 결정

1. **주문은 작품의 속성이 아니라 독립된 사건이다** — 행(row)이어야 한다
   - 한 작품에 주문이 여러 번 붙는다(취소 후 재주문, 실물+디지털)
   - 게임과 무관한 주문이 더 많다
   - 작품 DB는 이미 33속성 + 스크립트 8개가 쓴다. 늘릴 자리가 아니다
2. **그 행은 기존 구매DB에 둔다** — 새 DB를 만들지 않는다
3. **작품 DB는 복제하지 않고 rollup으로 비추기만 한다**

## 스키마

### 구매DB — 속성 5개 추가

| 속성 | 타입 | 용도 |
|---|---|---|
| `구입처` | select | 사줘 · 아마존재팬 · 쿠팡 · 알리 · 네이버 · 닌텐도스토어 · 스팀 · 오프라인 |
| `결제액` | number(원) | 실제 낸 총액. 미넬 골렘 = 56,212 (상품 45,057 + 배송 6,650 + 관세 4,505) |
| `도착예정` | date | **알림의 근거.** 예약구매는 발매일, 직구는 예상 도착일 |
| `주문번호` | text | 판매처 문의 시 필요 |
| `작품` | relation → 작품 DB | 게임·하드웨어일 때만. 비워도 무방 |

기존 `예상 가격`은 그대로 둔다 — 사기 전 견적이고 `결제액`은 산 뒤 실제값이라 다른 값이다.

**상태에 `예약주문` 추가** (`주문·배송 중` 앞)

```
고민 중 → 조사 중 → 구매 필요 → [예약주문] → 주문·배송 중 → 구매 완료
                                                            └→ 보류·취소
```

`예약주문`을 나누는 이유: 결제는 끝났지만 물건이 아직 세상에 없는 구간은
`주문·배송 중`과 성격이 다르다. 전자는 발매일까지 기다리는 게 정상이고
후자는 일주일 넘으면 이상하다. **알림 기준이 달라야 해서 상태를 나눈다.**

**카테고리에 `게임·하드웨어` 추가**

### 작품 DB — 2개만

| 속성 | 타입 |
|---|---|
| `구매` | relation → 구매DB (위 `작품`의 반대쪽, 자동 생성) |
| `주문상태` | rollup → 구매.상태 |

33 → 35. 어떤 스크립트도 건드리지 않는 새 이름이라 덮어쓰기 위험 없음.

## 입력 — 채팅 한 줄

```
"사줘에서 미넬 골렘 예약했어. 총 56,212원, 주문번호 26090964602"
```

세션이 `buy.py`를 돌린다. `apply_candidates.py`와 같은 패턴
("세션이 채팅에서 받은 결정을 노션에 반영한다").

```bash
python buy.py 주문 "amiibo 미넬 골렘 (티어스 오브 더 킹덤)" \
  --구입처 사줘 --상태 예약주문 --결제액 56212 \
  --주문번호 26090964602 --도착예정 2026-09-17
python buy.py 수령 "미넬 골렘"      # 상태 구매 완료 + 구매일 오늘
```

**relation은 제목으로 매칭하지 말 것.** 제목을 고치면 끊긴다(하드웨어 4건
개명 사례). 연결할 때 작품 page_id를 구매DB 메모에 같이 적어둔다.

## 배송 알림

**대상: 결제가 끝난 것 전부, 물건 받을 때까지.** 발매까지 얼마나 남았든
상관없다 — 이미 돈이 나갔으므로. 아직 안 산 것은 대상이 아니다.
접는 규칙 없이 전부 매일 띄운다(동시에 10건 넘을 일이 없다).

```
[배송] 진행 중 2건
  amiibo 미넬 골렘     예약주문 · 발매 9/17 · 5일 남음 · 사줘
  로지텍 키보드        배송 중 · 도착 예정 9/14 · 2일 남음 · 쿠팡
```

| 상태 | 표시 | 빈도 |
|---|---|---|
| 예약주문 | `발매 9/17 · 5일 남음` | 매일 |
| 배송 중 | `도착 예정 9/14 · 2일 남음` | 매일 |
| 도착일 당일 | `오늘 도착 예정` | 매일 |
| 예정일 경과 | 확인 큐(q번호)로 넘어간다 → 설계-확인큐 참조 | 매일 |
| 구매 완료 | 카드에서 빠짐 | — |

## 송장 추적 — `sync_delivery.py`

**스마트택배(sweettracker) API.** 사용자가 이미 같은 서비스의 앱을 쓰고 있다.
무료 키 필요.

```
송장번호 입력 → 매일 조회 → 집화·간선상차·배달출발·배달완료
             → 배달완료면 상태를 자동으로 `구매 완료`, 구매일 기록
```

국내 택배는 완전 자동이라 "왔어"라고 말할 필요도 없어진다.

**한계 (사줘 같은 구매대행)**

| 구간 | 추적 |
|---|---|
| 일본 현지 판매처 → 대행 창고 | 불가 (로그인 필요) |
| 국제 배송 | 불가 |
| 한국 통관 후 국내 택배 | 가능 |

지금 앱에서 수동으로 하는 것과 같은 한계다. 국내 송장이 나오기 전까지는
`도착예정` 날짜 기반으로만 표시한다.

## 함정

1. **통합 권한** — 구매DB 페이지에서 `⋯ → 연결 → 출시 트래커`를 사용자가
   직접 걸어야 한다. 이 설계에서 유일하게 대신 못 하는 단계다
2. **제목 매칭 금지** — 위 참조
3. **기존 16건은 손대지 않는다** — 새 속성이 비어 있어도 무해하다


## 하드웨어 감시 — 네이버 쇼핑 API 폐기, 뉴스 검색으로 대체 (2026-09-12)

원래 안(네이버 쇼핑 API로 국내 최저가·예약 개시 감지)은 **쓸 수 없다.**
네이버 검색 API 중 쇼핑·책·학술정보가 2026-07-31부로 완전 종료됐다. 뉴스·
블로그·카페는 NAVER API HUB로 이관돼 살아남았고, 개인 개발자에게 쇼핑
검색은 더 이상 발급되지 않는다.

**대안: 네이버 뉴스 검색 API로 하드웨어 제목 키워드 매칭.** 제품 페이지를
직접 긁는 것보다 낫다 — 페이지는 "지금 상태"만 보여줘 구조 변화에 취약하고,
뉴스는 "예약판매 시작"·"출시일 연기" 같은 **사건**을 직접 말해준다.

키는 새로 안 만든다. `place-finder` 애플리케이션(NAVER API HUB, 지역·블로그·
카페·지식iN 보유)에 `뉴스`(NAVER Search News API)만 체크 추가하면 된다.
사용자가 이미 소유한 것을 넓히는 것뿐이라 추가 발급 절차가 없다.

**날짜는 기사 본문에서 파싱하지 않는다.** 게임쇼 사전 예고 때와 같은
이유다 — "9월 25일 오후 11시"를 텍스트에서 정확히 뽑는 건 스크립트 혼자
자주 틀린다. 대신 이 리포가 이미 쓰는 원칙(`refresh_dates.py`: 감지만
하고 사람이 결정)을 그대로 따른다.

```
매일 하드웨어 7건 제목으로 네이버 뉴스 검색
  → 어제 없던 새 기사가 있으면
  → 확인 큐(설계-확인큐.md)에 후보로 올림: "이 기사가 관련 있을까요?"
  → 사람이 보고 반영/무시
```

미착수 — 착수 시 이 절부터 다시 볼 것.
